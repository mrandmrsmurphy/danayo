---
name: project-character-sweeps
description: "Closed - character-page perfection sweeps (danayo_id /loop thread, Components-driven thread, manual thread); all 3350 character files carry date-last-perfect stamps as of 2026-09-13"
type: project
---

**Status: closed 2026-09-13.** Every `characters/*.md` file (3,350) now carries a filled `date-last-perfect` stamp (verified by corpus-wide grep). The `danayo_id` thread's own log (`Loop Work.md`) ends at iteration 434 (2026-08-01, 該, id 4255), so the tail of the pool was swept in untracked sessions — same "swept but not logged" pattern as the Syllables sweep; those stamps are unverified against the checklist. The Components-driven thread's remaining targets were re-verified mechanically 2026-09-13: all targets extracted from `broken links output.md`'s Components section carry stamps.

## Original projects.md entries (archived)

- **Character-page perfection sweep, `/loop` batch (2026-07-16, new thread; resumed 2026-07-28)**: a self-pacing `/loop` picking up the never-perfected pool directly — `grep -L "^date-last-perfect: [0-9]" characters/*.md` currently returns 3032 of 3350 character files (most of the corpus has never been touched by this checklist at all). Working ascending by `danayo_id` (most fundamental/common characters first) rather than the manual/oldest-date ordering the 2026-07-03 thread below used. Full log with per-character defect notes lives in [[Loop Work.md]], not duplicated here — check that file for the current stopping point rather than this line, since the loop updates it every iteration. First iteration (2026-07-16) did 小 (id 23) and 口 (id 24), both had never been touched: notes bullets mashed together/missing the `../` path prefix, several Words entries missing ruby or missing outright, one word-file `characters:` filename-form bug class (bare `口` instead of `口 (char)`, found on 3 files), and two incidental content bugs fixed along the way (a word's English gloss typo, a Cantonese field with pinyin copy-pasted into it, one flatly wrong gloss unrelated to the character itself). Confirmed the `graphemic_classification` field is the right cheap check for whether a `## Derived Characters` section applies (grep for other characters naming this one as their phonetic component) rather than trying to enumerate every character that merely shares the radical. After iteration 23 (毎/気, 2026-07-16) the thread went unlogged for a stretch; resumed 2026-07-28 at iteration 24 with [[characters/年 (char)|年]] (id 82) — found already near-complete (only the semantic component's missing Radical-115 link on 禾 needed fixing), restamped after a full re-verification rather than assuming the existing stamp was trustworthy. Iteration 25 (肉, id 83): another already-stamped-but-not-quite-complete page — fixed a malformed `## Derived` placeholder (`- 育?`) into a real `## Derived Characters` entry (confirmed 育 genuinely names 肉 as its phonetic component), and found the recurring bare-filename bug (`- 肉` instead of `- 肉 (char)`, plus `- 豚`→`- 豚 (char)`) on 3 word files (狗肉, 筋肉, 豚肉) whose Etymology prose already had the correct link — only frontmatter was wrong. Iteration 26 (多, id 84): missing `../` relative-path prefix on the disambiguation callout and all of bullets 2/3/4 (SKIP/Stroke, MC, Levels) — otherwise fully complete (Words 16/16, Chengyu 2/2, Derived Characters 3/3 all matched ground truth exactly). **Major finding while computing the next pointer**: a full mechanical scan shows the entire contiguous `danayo_id` range 85–3107 is already stamped (mostly 2026-07-22 through 2026-07-25) — this whole stretch was swept in untracked sessions between iteration 23 (2026-07-16) and now, same "swept but not logged" pattern as the Syllables thread. 2433 of 3345 characters remain overall (down from "3032 of 3350" on 2026-07-16), but the remaining pool now starts at id 3108 and has gaps throughout up to the max id 8816, not a clean ascending run. Iteration 27 (序, id 3108): first character in the newly-discovered remaining pool, genuinely never touched — built the whole page from scratch (blank `pos`, no Notes bullets at all, no `## Words` heading). Verified `graphemic_classification: 予` via Wiktionary (形声, semantic 广 + phonetic 予, OC roots share an *l- onset despite MC z-/j- divergence). Iteration 28 (式, id 3109): a subtler frontmatter/body contradiction than usual — field (`弋`=phonetic) and prose (claimed 弋=semantic, 工=phonetic) disagreed on *role assignment*, not just which character; Wiktionary + OC rhyme evidence confirmed the frontmatter was right and the prose backwards (弋 *lɯɡ rhymes with 式 *hljɯɡ; 工's own OC reading is a different rhyme class entirely, ruling it out as phonetic). Also caught a guessed-ruby near-miss before shipping (方程式's old broken entry had a P-initial `<rt>` when the word file's own stored 注音 is F-initial). Iteration 29 (律, id 3111): never touched, built from scratch. Phonetic component 聿 has no character page in this vault — same unlinkable-component precedent as 気/气, 左/𠂇, 卑/𠂇 (described in prose, not wiki-linked). Found and added a missing chengyu (金科玉律) alongside the already-present 千編一律. Next: 徒 (3112), 復 (3113), 徳 (3114) — see [[Loop Work.md]] for the up-to-date stopping point.
- **Character-page perfection sweep, Components-driven thread (2026-07-17, new thread)**: per user request, prioritizing the 152 characters listed as "in" targets under `## Components` in [[broken links output.md]] — each line there is a broken-link component (e.g. `[[卩]]`, `[[丨]]`) followed by the vault characters that reference it in their own graphemic bullet. The user's insight: many of these "missing" components aren't missing characters at all, just Kangxi radicals that were bare-linked instead of pointed at their `Lookup/Radicals/Radical NNN.md` page — running the standard perfection checklist (which already has the radical-linking rule, see `checklist_characters.md` bullet 1) on the *consuming* character resolves the false-positive broken link as a side effect, without needing to create a character page for something that was never a character. Distinct ordering criterion from both threads below — don't conflate "last one done" pointers. Full log lives in [[Loop Work.md]] under its own thread heading. Done so far (2026-07-17): 光 (fixed bare `[[卩]]`/`[[火]]` component links to `[[Radical 026|卩]]`/`[[Radical 086|火]]`, rebuilt Words 6→11 entries + added missing Chengyu section + Derived Characters 恍/洸/晃/胱), 即 (same 卩 fix, reordered Words before Chengyu, added missing 即/即日 words + 一触即発 chengyu + Derived Characters 節, split a merged SKIP+MC bullet into the canonical two), 皮 (`graphemic_classification` was the bare tag `為` — a plain data error, no such classification exists — corrected to `象形` per Wiktionary and rewrote the bullet from stale "Components:" text; added missing `>[!tip]` callout since it had none; Words 2→9 entries; added Derived Characters for the well-attested 彼/披/破/被/頗/波/玻/疲 phonetic series, each gloss pulled from the character's own `english:` field rather than guessed), 色 (same 卩 fix, added missing callout + Levels bullet, Words 19→32 entries — largest gap-fill this thread so far — Derived Characters 赩), 見 (merged a duplicate `## Notes` heading, fixed a `graphemic_classification: 指事` vs. body-text `会意` contradiction, built Words/Chengyu/Derived Characters from scratch — Derived Characters 現/硯), 刈 (the broken link `[[㐅]]` was a rare variant glyph of 乂, which already has its own perfected character page in this vault — repointed the bullet at the real target instead of creating anything new; also found and fixed a genuine gap in `checklist_characters.md`'s Level-mapping table, `grade_level: 先進` → `[[Grade Advanced]]`, undocumented until now), 千 (user-flagged as a real etymology error, not just a broken link — `graphemic_classification: 遷` and the bullet both wrongly claimed phonetic 遷; Wiktionary confirms the real phonetic component is 人, an extra stroke added to distinguish the numeral, independently corroborated by [[characters/年 (char)|年]]'s own already-perfected bullet — corrected the field and bullet, added the missing `## Derived Characters` (年); also fixed 5 Chengyu entries missing ruby/rt), 卑 (its broken `[[𠂇]]` link is a genuinely unlinkable no-page component, same class as the earlier 気/气 and 左/𠂇 precedents — described in prose instead of wiki-linked; existing 会意 etymology confirmed correct via Wiktionary, no fix needed there; promoted a wrongly-nested `### Descendants` to `## Derived Characters` and expanded 1→6 real hits, a well-attested p/b-initial phonetic family), 反 and 雁 (shared the same broken `[[厂]]` → `[[Radical 027|厂]]` fix; 反 also surfaced a real unresolved tension — `words/反乱.md` uses `叛`, not `反`, in its `characters:` field despite 反's own page saying 叛 is folded into it, yet the word's actual stored reading is built on a completely different first syllable, meaning 叛 evidently keeps its own independent Dan'a'yo phonetic identity somewhere — flagged, not resolved, see [[Loop Work.md]] for detail; also caught and fixed two batches of guessed-not-verified rubies/glosses before shipping, and a "fannie"→"fanqie" typo on `words/反切.md`. 雁's old "Components: 厂, 倠" text linked a nonexistent, non-existent-in-Wiktionary-either glyph 倠 — replaced with the real 厂+人+隹 breakdown), 受 (found a genuine `graphemic_classification` bug — field said `舟`, wrongly asserting 形声 with phonetic 舟, but Wiktionary's real analysis is 会意 (爪+舟+又, "to hand over via a boat") with 舟 corrupted visually into what looks like 冖 in the modern glyph; corrected the field to `會意`; also linked 爫→Radical 087 as a variant-glyph case like 反/雁's 厂, and retroactively fixed the same missed 爪→Radical 087 link on `色.md` from two iterations ago). 141 characters remain on the list; recommend running the rest via `/loop` given the scale, same pacing pattern as the `danayo_id` thread.
- **Character-page perfection sweep, manual thread (2026-07-03)**: going through existing `characters/` files one at a time and bringing each to full `date-last-perfect` compliance against [[AIOS/checklists/checklist_characters.md|checklist_characters.md]]'s 10-point checklist (frontmatter completeness, all four Notes bullets in order, complete Words/Chengyu sections with ruby+gloss) — independent of the wiki, no wiki lookup involved. Distinct from the `/loop` thread above (different ordering, different session) — don't conflate the two "last one done" pointers. Last one done: 仰 (fixed blank `pos`, missing Notes bullets 2-4, unformatted Words entry; phonetic component 卬 has no character page and isn't a radical, so — per user, don't create new characters to unblock another unless there's no other way — it was aliased onto [[卯]] instead, backed by Wiktionary's Etymology 4 for 卬 as "variant of 卯"; removed a conflicting pre-existing `卬` alias claim that had been sitting on 仰's own page). Before that: 代 (blank `pos`, wrong `graphemic_classification`, missing Notes bullets 2-4, several missing/unformatted Words+Chengyu entries; phonetic component 弋 *is* a radical, so was linked via its Radical page). Note: 昂 and 迎 also reference `[[卬]]` as their phonetic component and remain unperfected — now resolvable via 卯's alias, but not yet done. Also done: 保 (graphemic bullet was leftover wiki-style "Components:" text, not proper 会意 format; `### Derived Characters` was wrongly nested as H3 inside Notes instead of its own `##` section; `褓` was miscategorized as an alias — same category error as 卬/仰 — moved to Derived Characters, left unresolved since 褓 has no page; `## Words` section didn't exist at all, just 4 bare bullets missing 3 real words and the stand-in itself). Also done: 俯 (graphemic bullet was leftover wiki-style "Components:" text; needed a new supporting character 瞰 created — no CC/HSK presence, phonology borrowed from 敢/嵌 analogs — plus the new word 俯瞰 itself, since neither existed; added 瞰 as a 6th entry on the ㄎㄚㄇ syllable page). Note: that syllable page's `date-last-perfect` was already set despite its own entry 5 (坎) being an unresolved link to a nonexistent character — pre-existing bug, flagged but not fixed. Also done: 厄 (graphemic bullet was a bare "Components:" list; kept existing 指事 classification rather than switching to Shuowen's alternative 形声 reading; "Derived characters" was loose unheaded text, promoted to proper `## Derived Characters`; no `## Words` heading existed). Also done: 厠 (graphemic bullet was "Components:" text; 厂 here is a simplified stand-in for 廁's real semantic component 广 "building/shelter," noted explicitly; radical linked correctly this time per the corrected BP rule). Resume with the next character the user names.
