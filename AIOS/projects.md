# Projects

Living status document for ongoing work on the Dan'a'yo vault. Read this at the start of every session to see what's active and where to resume. Deeper history for any project lives in its own `projects/project_*.md` file, linked below.

## Active

(none currently)

## Active

- **MediaWiki migration, resuming (2026-06-20)**: paused the `## More` word-creation sweep below to switch back to character-by-character wiki migration (compare `danayo.conlang.org` legacy pages against vault `characters/`). Last character confirmed/fixed was 缸 (all fixes applied, safe to delete wiki page — user deletes himself after confirming each). Resume with the next character in sequence.

## Closed / historical

