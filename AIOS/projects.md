# Projects

Living status document for ongoing work on the Dan'a'yo vault. Read this at the start of every session to see what's active and where to resume. Deeper history for any project lives in its own `projects/project_*.md` file, linked below.

## Active

(none currently)

## Active

- **`lint Stroke` development (2026-07-09, new)**: drafted the `lint Stroke` procedure into [[AIOS/skills/skill_lint.md|skill_lint.md]] (grounded in [[AIOS/checklists/checklist_stroke.md|checklist_stroke.md]], which — unlike the Chengyu checklist — was already thorough and well-specified going in, so this was scoping-by-sampling rather than reverse-engineering a rubric from scratch). Not yet validated against a live batch. `lookup/Stroke/` is 31 files: 30 stroke-count pages (`Stroke 01`–`Stroke 30`) + `Stroke.md`. Two structural surprises found while scoping: **`Stroke.md` is not a grouping/index file** for the 30 leaves despite every leaf's breadcrumb linking to it — its actual content is an unrelated calligraphy reference (永字八法, brush-stroke type taxonomy), so unlike SKIP/Radicals/Chengyu there's no "verify grouping-file membership" step for this lint type at all; and **character volume per page varies by two orders of magnitude** — `size` ranges from 0 (`Stroke 30`, legitimately empty) to 337 (`Stroke 12`), with several pages in the 200–340 range, the largest single-page volume any lint type has had to handle. Batches need to flex accordingly: one large page per fork, several tiny ones bundled together.
  - Sampled `Stroke 01.md` and `Stroke 25.md` (both dated 2026-02-22/23, the oldest stamps in this tree) and found **all five of the checklist's own documented "Common mistakes" present simultaneously on both** — numbered list instead of SKIP-code bullets, SKIP codes omitted entirely, English glosses appended (explicitly forbidden), `### Data check` with a non-canonical query shape instead of `## Data check` with the canonical one, and an unclosed ` ```dataview ` fence (the same fence-closure bug now confirmed in Radicals, Chengyu, *and* Stroke — a genuinely cross-cutting vault-wide defect class). By contrast, `Stroke 12.md` (dated 2026-04-27, the batch covering the large stroke counts) was already fully checklist-compliant on inspection — so this is very likely another "old batch bad, newer batch fine" split, not uniform corpus-wide rot.
  - **First real test (2026-07-09): `Stroke 04.md` (size 88, part of the 2026-04-27/28 batch), checked in full — zero defects.** All 88 characters and all 12 SKIP-code groups matched ground truth exactly (verified every entry, not a sample): correct grouping, correct ascending SKIP order, `size` correct, every pipe-alias-vs-plain-wikilink choice correct across all 88, and a 10-character `注音` spot-check against each character's own file found no typos. Re-stamped `date-last-perfect` to reflect the verification even though no content needed to change. This is a good sign for the 2026-04-27/28 batch generally (which covers the huge stroke-count pages) but is only one data point at the small end of that batch's size range — still needs checking page by page, not assumed clean.
  - **Second real test (2026-07-09): `Stroke 17.md` (size 102) — a real fix.** User spotted the tell before any grep ran: the page's own Data check table returns 104 rows against a stated `size: 102`, a fast, cheap triage signal worth using across a whole batch before committing to a full audit on each page. A mechanical name-diff (extract every linked character, strip `(char)`/pipe-alias noise, set-diff against the grep'd ground truth) found exactly 3 missing characters in one pass: `屿` (a whole new SKIP-1-3-13 group nobody had added) and `瞬`/`瞰` (missing from the existing 1-5-12 group) — two of the three have high `danayo_id`s, i.e. genuine corpus growth since this page was last touched, not a formatting mistake. Also caught: the page's *actual* content was 101 entries even before fixing, one less than the already-stale `size: 102` — the stated size was wrong even for its own prior content. Fixed all three, `size` corrected to 104, re-verified zero gaps remain, re-stamped. Distinct failure mode from the 2026-02-22/23 batch: format was already fully correct here, this page just needed new content backfilled.
  - **Third real test (2026-07-09): `Stroke 02.md` (size 16, dated 2026-02-22, deliberately picked as "an old junker") — full format rewrite, content was already complete.** Confirmed the predicted 2026-02-22/23 defect signature exactly: `#### SKIP-code` H4 headers over a numbered list instead of unordered SKIP-prefixed bullets, English glosses on every entry (some doubly redundant, repeating the SKIP code again in parens after the gloss), an unclosed ` ```dataview ` fence, and a non-canonical Data check query. New specific bug variant: one entry (了) linked to the *word* file (`words/了.md`) instead of its own character page, despite `characters/了 (char).md` existing — same "wrong link target" class as the Chengyu character-backlink mistakes, now found here too. Ground-truth diff showed all 16 characters were already present — nothing missing, nothing extra — so this was a pure format rewrite, the opposite imbalance from `Stroke 17` (which had correct format but missing content). Rewrote, re-verified 16/16 with zero discrepancies, stamped.
  - **First real batch (2026-07-09): the 9 smallest remaining pages — `Stroke 01`, `23`–`30`** (sizes 0–7, all 2026-02-22/23-dated), fixed directly rather than forked out given the small scale. All 9 shared the predicted old-batch signature at varying severity: missing `## Characters`/`### In Use` headers (bare bullets on most), no SKIP codes on entries, English glosses throughout, unclosed `dataview` fences on all 9, non-canonical query shape (wrong columns, `file.name` sort) on all 9. Assorted one-offs: `Stroke 27` used ad-hoc `## Gold`/`## Fish` headers instead of real structure; `Stroke 29` had a stray "That's it!" line; `Stroke 30` (legitimately `size: 0`) had aliases under `## Redirects` instead of `### Aliases`; `Stroke 24`'s 鷹 entry used `[[鷹 (char)]]` with no pipe-alias (would render the literal string "鷹 (char)" as link text). **The one real content gap: `Stroke 24` was silently missing 3 characters** — 驟, 衢, and 鑫 (the modern folk-coined character from `skill_character_creation.md`'s own documented example) — and its stated `size: 4` matched its own incomplete content exactly, so the size-vs-Data-check triage trick from `Stroke 17` didn't catch this one; only the mechanical ground-truth diff did. All 3 added, `size` corrected 4→7. All 9 re-verified: ruby-tag count matches ground truth exactly on every page, every fence confirmed closed.
  - **Top-of-scale test (2026-07-09): `Stroke 10.md` (size 318→321)** — user flagged "clearly missing three characters, otherwise pretty good, can't fully vouch for it." A full mechanical diff (321 ground-truth characters vs. 318 on the page, across all 33 SKIP groups) found exactly 3 missing (`䋇`, `紐`, `垂`) and confirmed zero extras/misfilings anywhere else — the user's read was exactly right. Two of the three slotted into an existing group (`1-6-4`); the third (`垂`) needed a brand-new `4-8-2` group appended at the end. Good validation that the `grep`-ground-truth + `comm`-diff technique scales cleanly all the way to 300+ characters with no adaptation needed. `size` corrected, re-verified, stamped.
  - **Correction (2026-07-09): `垂` wasn't actually missing from `Stroke 10` — the user caught it immediately ("垂 is an 8 stroke character!").** Verified against EDRDG: `垂`'s SKIP code (`4-8-2`) was already right, but `characters/垂.md`'s own `stroke_count: 10` was wrong (should be 8) — a genuine corpus data error, not a lint-page gap. This is the real limit of the mechanical-diff technique: it only checks a page against whatever the corpus's `stroke_count` fields currently claim, not whether those claims are true. Fixed the character file, moved the entry to `Stroke 08` (the page it actually belongs on), and — since that meant a real ground-truth check on `Stroke 08` for the first time — found **4 more pre-existing gaps unrelated to 垂**: `刮`, `奈`, `拙`, and `怯` (created/renamed earlier this same session during the Syllables lint work, never backfilled here). `Stroke 08`'s `size: 281` had been wrong by 4 already, independent of 垂. All 5 added, both pages' `size` fields corrected (`Stroke 10` 321→320, `Stroke 08` 281→286), both re-verified clean. Lesson for future batches: a mislabeled `stroke_count` fix always touches two pages, and the destination page deserves a full ground-truth check, not just the one new addition — an unrelated gap there is easy to miss otherwise.
  - **Corpus-wide cascade (2026-07-09): the user supplied a domain-knowledge shortcut — "SKIP-4 always has a second number that is the stroke count"** (unlike categories 1-3, whose two numbers are a decomposed breakdown) — spotting `垂`'s error on sight, without opening a file, just by noticing `4-8-2` didn't match `stroke_count: 10`. This is mechanically checkable across the whole corpus in one grep/case pass comparing every `4-*` `skip_number`'s second digit against that same file's `stroke_count`. Running it found **2 more instances of the identical bug class**: `乎` (`skip_number: 4-5-3`, `stroke_count` was 8, should be 5) and `乗` (`skip_number: 4-9-3`, `stroke_count` was 12, should be 9) — both confirmed via EDRDG (SKIP correct, `stroke_count` wrong both times), both fixed. Cascade: `乎` removed from `Stroke 08` (286→285) and added to `Stroke 05`; `乗` removed from `Stroke 12` (337→336) and added to `Stroke 09`. Per the just-established "check the destination page fully" lesson, full ground-truth diffs were run on **all four** touched pages (`Stroke 05`, `Stroke 08`, `Stroke 09`, `Stroke 12`) — every single one turned up something beyond the one character that prompted the visit: `Stroke 09` had 3 more unrelated pre-existing gaps (`兗`, `恍`, `後`, final size 297); `Stroke 12` had 3 more (`堡`, `徨`, `跋`, final size 339, plus a non-canonical Data check query column fixed); `Stroke 05`'s stated `size: 122` turned out to already be off by one even before `乎` was added (true prior count was 121, final correct size 122 after adding `乎`) — a reminder that a stale `size` field can't be trusted even via simple arithmetic, only a fresh ruby-count-vs-ground-truth comparison is reliable. All four pages (`05`, `08`, `09`, `12`) fully re-verified clean and re-stamped `2026-07-09`.
  - Next step: continue through the remaining confirmed-bad 2026-02-22/23 pages, and/or try another page from the large end of the scale to build more confidence there. The SKIP-4 corpus-wide check could in principle be re-run to catch any remaining instances beyond the 3 found so far (`垂`, `乎`, `乗`), though none are currently known to remain.

- **`lint Chengyu` development (2026-07-09, new)**: drafted the `lint Chengyu` procedure into [[AIOS/skills/skill_lint.md|skill_lint.md]] (grounded in [[AIOS/checklists/checklist_chengyu.md|checklist_chengyu.md]]) — the first `lint` type after Syllables, and structurally different from all three prior ones: `chengyu/` (229 files: 226 idiom pages + 3 grouping files `Misc.`/`Dan'a'yo`/`Biblical Chengyu.md` + one `.base` query) has *every* leaf already carrying some `date-last-perfect` stamp, so "oldest first" doesn't surface the worst pages the way it did for syllables; each page is a genuine encyclopedic article, not a mechanically-derivable index entry. Validated against 4 hand-picked pages (established `創反救成` as the gold-standard reference; `愛隣如自` as a weak-page example), 2 truly-random picks (`五風十雨`, `国士無双`), and then the 6 chronologically-oldest pages by the user's own request (`一刀両断`, `鶏鳴狗盗`, `空中楼閣`, `魑魅罔両`, `開天辟地`, `心性意力`) — **all 8 fully fixed and stamped `2026-07-09`** as a live test of the procedure, not just diagnosed.
  - **Confirmed recurring defect classes**: `###` used instead of the required `##` for body sections (near-universal); the category link missing entirely and replaced by a bare `# Title` heading; pages inventing non-canonical section taxonomy instead of the checklist's 8 (splitting Cultural Notes into two overlapping sections, adding an undocumented "Related Chengyu" section duplicating entries already under "Variants"); character backlinks broken at a very high rate, including a "present but malformed" sub-case (backlink exists but missing ruby and/or gloss, sometimes an absolute path) — found on `十`/`士`/`中`/`天`/`力`'s own pages across this batch, not just the random sample; `characters:` frontmatter list using the wrong filename form (missing `(char)` suffix) — found on 3 separate pages (`五風十雨`'s 十, `開天辟地`'s 天+地, `心性意力`'s 心); a `characters:` list entry that's a bare scalar or a body section with a fabricated/contradicted origin story (`一刀両断`'s `origin` cited a specific 1925 Zhu Ziqing essay by name — verified genuine via web search, quote included in essay — but the body's old Source section ignored it and told an unrelated generic story; rewritten to actually cite it).
  - **Real content-accuracy bugs caught, not just structural ones**: `魑魅罔両`'s frontmatter `korean` field was wrong (이매망량, should be 치매망량 — verified via each character's own real Korean reading; the body's Pronunciation section already had it right, frontmatter didn't) and its body Pronunciation had a Cantonese typo (wong5→mong5, confirmed against `罔`'s own page). Also surfaced but *not* touched, flagged only: `両(char)`'s own Cantonese field (loeng2) conflicts with the compound-level loeng5 used consistently in both `一刀両断` and `魑魅罔両` — turns out 兩 ("two/both," loeng5) and 両-as-"tael" (loeng2) are different senses with different real Cantonese readings, and the vault's `両` character page appears to conflate them; a genuine character-level disambiguation question for a dedicated pass, not something to silently "fix" by breaking the compounds. Also: `辟`(char)'s and `両`(char)'s own aliases already correctly list 闢/魎 as their classical full forms — the vault's simplified-stand-in-grapheme convention was already sound, just needed the chengyu pages to say so explicitly in Literal Meaning.
  - Also fixed in passing, low-effort/high-value: duplicate `## Notes` headings on `堡.md` (earlier) and `魑.md`; several stray empty bullet-line artifacts (`一(char)`, `十(char)`, `狗`).
  - **Third batch (2026-07-09)**: 5 pages chosen to span all three `origin` categories per the user's request — `現代適応`/`日用必備` (単亜語), `戦戦恐恐`/`跳梁跋扈` (classical: 詩經/後漢書), `創反救成` (Bible, already the established gold-standard reference, just tidied). All fixed and stamped. This batch was dated much more recently (2026-06-19 through 2026-07-01) than the earlier samples and was correspondingly far cleaner: **all 18 character backlinks across all 5 pages were already correct** — first batch with zero backlink defects — confirming recency correlates with quality and lint effort should scale down accordingly for newer pages, not stay uniformly heavy. Remaining defects were smaller: near-canonical-but-not-exact section names (`## Extended or idiomatic sense`→`## Extended Meaning`), a reasonable non-canonical `## Invocation` section remapped to the checklist's optional `## Usage Note`, missing `## Cultural Notes` on both Dan'a'yo-coined pages (added as an explicit "this is vault-internal metalanguage, no real cross-CJKV variation" note rather than forcing invented comparative content), and a `vietnamese`-field class of bug: `戦戦恐恐` substitutes 恐 (kǒng) for classical 兢 (jīng) throughout every other reading, but its `vietnamese` value had been sourced for the *classical* 兢-form ("căng") instead of matching the actual 恐-based substitution ("khủng") — caught by checking 恐's own character-level field. Also filled in two genuinely-blank constituent-character `vietnamese` fields (`戦`→"chiến", `跋`→"bạt", both verified via search) that were the root cause of compound-level gaps. `日用必備` and `戦戦恐恐`/`跳梁跋扈` were also missing from their grouping files entirely (added), and a `Dan'a'yo Chengyu.md` malformed doubled `</ruby></ruby>` tag (on `合漢再決`, unrelated to this batch's own pages but caught while editing the file) was fixed in passing.
  - **Fourth batch (2026-07-09)**: 5 more chronologically-oldest pages — `一刻千金`, `一長一短`, `五臓六府`, `優柔不断`, `切磋琢磨` (all tied at `2026-02-03`, the next-oldest date bucket after the first oldest-batch). All fixed and stamped. These 5 turned out to share an obvious "house style" distinct from every prior batch: identical non-canonical section names across all five (`## Meaning`, `## Source`, `## Background`, `## Modern`, `## Pronunciations across CJKV`, closing `**Usage note:**`), and — new, more severe than anything found before — **all five were missing `## Example Sentences` and `## Sentiment` entirely**, not mis-named, just never written; had to compose new examples and sentiment lines from scratch for all 5, not just relocate existing content. Also found: `五臓六府` had a stray `[[五臓]]+[[六府]]` line sitting *before* the `meta-bind-embed` block (direct checklist violation); a genuine `注音`/`羅馬字`/`諺文` typo on the same page (`ㄆㄨ`/`pu`/`푸` instead of the correct `ㄈㄨ`/`fu`/`뿌` — 府, used as a stand-in for 腑, has an F-initial reading, but the frontmatter had a P-initial bopomofo letter that looks nearly identical, `ㄆ` U+3106 vs `ㄈ` U+3108); and a malformed backlink on `臓`'s own page filed as `### Chengyu` nested inside `## Words` instead of its own `## Chengyu` section. Two more wrong `characters:` filename forms (`切磋琢磨`'s 切 and 磨, both missing `(char)` suffix). All 17 unique constituent characters across the 5 pages backlinked (several missing outright, 3 malformed-but-present fixed in place), and all 5 added to `Misc. Chengyu.md` (none had been listed there before).
  - Procedure is still marked early, but now validated against 18 real pages across 4 sampling methods (2 hand-picked-weak + 2 hand-picked-strong, random, oldest ×2 batches, random-spanning-origins). Next: a genuinely larger batch to see whether it holds up at volume.
  - **Explicitly deferred milestone**: fully perfecting the three grouping files (`Misc.`/`Dan'a'yo`/`Biblical Chengyu.md`) as pages in their own right — none currently has a real `date-last-perfect`, and `checklist_chengyu.md` doesn't yet define completion criteria for a grouping file at all (unlike `checklist_radicals.md` for `Radicals.md`). Each leaf-level fix already keeps the relevant grouping file's membership list in sync incrementally, but a real completion pass only makes sense once most/all of the 226 leaves are done (13 so far) — attempting it now would risk the same stale-but-stamped-index problem the Radicals sweep found. When the leaf sweep is much further along, `checklist_chengyu.md` will need a new section defining grouping-file completion, mirroring the Radicals checklist.

- **Syllables lint sweep — full backlog now stamped; drift-triage pass complete (updated 2026-07-17)**: as of this date, all 896 `syllables/*.md` pages carry a filled `date-last-perfect` (dates now range through `2026-07-12`) and zero `dataviewjs` placeholders remain — the March/April-only snapshot below is stale; the rest of the backlog was evidently swept in sessions between `2026-07-09` and now that weren't logged back to this file. **A filled stamp is not proof of correctness, same lesson as `lint Stroke`/`lint Chengyu`** — corpus growth (new characters, renamed `stand_in`s, characters whose own `注音` changed after the page was last touched) silently drifts an already-stamped page out of sync. A cheap mechanical triage (compare each page's frontmatter `size` against a fresh ground-truth count of characters whose `注音` matches that code today) found **32 of 893 codes drifted** as of 2026-07-17; **31 fixed and restamped `2026-07-17` in this pass, 1 left flagged and then resolved** (`ㄆㄨㄎ` — page cited `[[攴]]` but no character file existed; per user instruction (2026-07-17), created [[characters/攴.md]] as a `名専字` naming-restricted character — 象形 pictograph of a hand gripping a rod, "to knock, to strike," `danayo_id: 8816`, `mc_id: 2777`. Its real MC derivation (pʰ+屋一, same class as [[撲 (char)|撲]]) would place it at `ㄆㄛㄎ`; placed at `ㄆㄨㄎ` instead as a **deliberate syllable-placement override** — same pattern as [[懍]], see [[feedback_syllable_placement_override]] — documented explicitly in the character's own MC-rank Notes bullet. `ㄆㄨㄎ.md` restamped. Downstream lookup pages updated for consistency: `Radical 066.md` (+0 Strokes group added — it can now claim entry 1 since 攴 is a real character file; size 20→21), `Radicals.md`'s count, `SKIP-2-2-2.md` (size 17→18), `Stroke 04.md` (size 88→89). **Deliberately not touched**: `聲 滂.md`/`韻 屋一.md` (the CC initial/final analytical pages) — since 攴's placement is an assigned override rather than organic sound change, adding it there would need the same exception-diagnosis treatment as 頗/尗/偪 and risks disturbing their already-reasoned, already-stamped prose; flagged rather than edited.
  - **Two distinct drift patterns emerged, in roughly equal numbers**: (a) a character was **added or reclassified** and never backfilled onto its syllable page (`ㄅㄚㄎ` +魄, `ㄅㄧ` +痺, `ㄊㄝ` +梯, `ㄋ⼘ㄫ` +攘, `ㄍ⺢ㄋ` +慣, `ㄍ⼄ㄨ` +狡, `ㄍ⼘ㄋ` +疆, `ㄍ⼜ㄛ` +舅, `ㄙ⼄` +屿, `ㄙㄚㄨ` +嫂, `ㄛㄨ` +欧, `ㄉㄧ` +値, `ㄙㄨㄊ` +朮); (b) a character's **own `注音` was later corrected/changed but the old syllable page was never updated to drop it**, leaving a stale duplicate entry that's already correctly present on the character's *real* current syllable page — confirmed and removed from: `ㄅㄚㄊ` (髪→ㄈㄚㄊ, 捌→ㄆㄚㄊ, 犮→ㄅ⼘ㄊ), `ㄇ⼄` (妙→ㄇ⼄ㄨ), `ㄇ⼶` (明→ㄇ⼶ㄫ, 面→ㄇ⼶ㄋ), `ㄍ⼶ㄫ` (繭→ㄍ⼶ㄇ), `ㄐㄨㄊ` (櫛→ㄐㄜㄊ, 朮→ㄙㄨㄊ), `ㄐㄨㄛ` (就→ㄑ⺢ㄧ), `ㄚㄋ` (恩→ㄜㄋ), `ㄧㄫ` (凝→ㄜㄫ). Pattern (b) is the more concerning one for a future pass: it means a page that looks internally consistent (a real character, correctly formatted) can still be wrong, because ground truth itself moved out from under it — always re-derive ground truth fresh per page, never trust an old batch's character list even for entries that "look fine."
  - **One more genuine corpus gap found the same shape as `攴`** (character cited but never created, distinct from a misfile): `鷲` (removed from `ㄐㄨㄛ`, no character file anywhere). **`㐮` turned out not to be this at all** (correction, 2026-07-17, per user): it's an alias glyph of [[攘]], already correctly listed in `攘.md`'s own `aliases:` field — the `ㄙ⼘ㄫ` entry was doubly wrong, both treating an alias as its own standalone character *and* misplacing it (攘 and its alias both belong on syllable `ㄋ⼘ㄫ`, not `ㄙ⼘ㄫ`). Removing it from `ㄙ⼘ㄫ` was still the right fix; no new page needed. Plus **word-level gaps** where the character exists and is correctly placed but its cited `stand_in` compound has no `words/` file yet, so the entry was added without a ruby rather than fabricating one: `熨斗` (for 熨 on `⼜ㄊ`), `狡猾` (for 狡 on `ㄍ⼄ㄨ`), `边疆`/`邊疆` (for 疆 on `ㄍ⼘ㄋ`), `呪文` (for 呪 on `ㄐㄨㄛ`), `島嶼` (for 屿 on `ㄙ⼄`), `兄嫂` (for 嫂 on `ㄙㄚㄨ`), `梯子` (for 梯 on `ㄊㄝ`) — seven ready-made word-creation tasks for a future `skill_word_creation.md` pass, each already fully diagnosed (character frontmatter confirmed, compositional 注音 derivable from constituent characters).
  - **New character-vs-word `注音` mismatches found** (same bug class as the 屎/破/即 cases from the original sweep — the character's own frontmatter is ground truth for page placement, but its compound's stored `注音` can diverge; not fixed here, just logged): `恩` (character `ㄜㄋ`, but `恩恵` stores `ㄚㄋㄏ⼔ㄧ`), `慣` (character `ㄍ⺢ㄋ`, but `習慣` stores `…ㄍ⺢ㄇ` for its contribution), `調` and `酒` (both characters read `ㄐㄨㄛ`, but `調整`/`酒精` both store `ㄐㄨㄐㄝㄫ` — missing the `ㄛ`; the identical failure shape on two unrelated compounds suggests one systematic bug rather than two coincidences, worth a dedicated look).
  - **Structural/formatting defects fixed along the way**, independent of the size-drift itself: non-canonical `諺文`-filtered Data check queries (often with a dead `OR 諺文 = "..."` duplicate clause) rewritten to the canonical `注音`-filtered form on `ㄅㄚㄊ`, `ㄌㄚ`, `ㄅㄚㄋ`, `ㄙ⼘ㄫ`, `ㄐㄨㄛ`, `ㄐㄧㄆ`; missing `### Common`/`### Advanced`/`### Naming` subsections rebuilt from a flat numbered list on `ㄎㄚㄇ`, `ㄎ⺢ㄊ`, `ㄐㄧㄆ`, `ㄑㄧㄎ`, `ㄙ⺢ㄫ`; bare-scalar `english:` frontmatter (not a YAML list) fixed on `ㄎ⺢ㄊ`, `ㄑㄧㄎ`; self-contradicting or incomplete three-script intro lines fixed on `ㄅㄚㄋ` (script order reversed), `ㄍ⼶ㄫ` (romanization didn't match frontmatter and `諺文` was missing from the citation entirely), `ㄎ⺢ㄊ`, `ㄑㄧㄎ`, `ㄙ⺢ㄫ`, `ㄊㄝ`; a non-canonical `## Dataview` section heading fixed to `## Data check` on `ㄅㄚㄧ` (this alone had been silently defeating any script that looks for the canonical heading to bound the Characters section).

- **`2026-05-01`-dated batch, full checklist audit (2026-07-17, user-directed)**: after the drift-triage above, the user asked to specifically sweep every page still stamped `2026-05-01` (31 files) — a good instinct, since 2 of the 32 drifted pages found earlier (`ㄐㄧㄆ`, `ㄐㄨㄛ`) were already known to be from this exact date and had real defects beyond simple count drift. Confirmed this is a real "house style era" batch, same phenomenon as the Chengyu lint's 2026-02-03 batch: **20 of the 31 pages had zero `### Common`/`### Advanced`/`### Naming` subsection structure at all** (flat numbered lists directly under `## Characters`), and **every single page in the batch had an intro line missing the full three-script `注音/諺文/羅馬字` citation** (most read as bare `**注音** means "X" (char)` with no romanization or hangul at all) — the two defect classes co-occurred on nearly every file, a clean signature for this date stamp specifically. Also found, scattered through the batch: bare-scalar `english:` frontmatter instead of a YAML list (most of the batch); several "no intrinsic meaning" pages using ad-hoc phrasing (`"lacks intrinsic meaning when used alone"`, `"...and is just used for sound"`) instead of the checklist's exact literal template (`> **注音** has no intrinsic meaning.`); a `但 it's a` naming clause truncated mid-sentence with no `名専字` on the end (`ㄋㄨㄎ`'s 衂 entry); a bound entry with the gloss dropped entirely (`ㄉㄨㄊ`'s 炢 — filled in from its own `english: dud`); several entries using the stand-alone `-->` arrow for characters that are actually bound (`ㄙㄚㄫ`'s 祥/笙, `ㄙㄝㄎ`'s 釈/潟 — all had a real ruby already, just the wrong connective text) — fixed to `but requires <ruby>...`. All 31 files reviewed; 29 fully fixed and restamped `2026-07-17`. **2 left on their original stamp, gaps flagged, not fabricated**: `ㄙㄨ`'s 鬚→`鬚髥` and 讐→`私讐`, both cited without a ruby pending investigation. **Correction (2026-07-17): `鬚髥` was never actually missing** — `words/鬚髯.md` already exists (dated `2026-06-03`, fully complete, ruby `ㄙㄨㄋㄛㄇ`), and `characters/髯.md`'s own `aliases:` field already lists `髥` as its variant glyph. The real bug was `characters/鬚.md`'s own `stand_in` field citing the alias form (`鬚髥`) instead of the canonical filename form (`鬚髯`) — fixed the `stand_in` field and the `ㄙㄨ` syllable-page citation (now properly ruby'd). `讐`→`私讐` is a genuine gap, confirmed by checking `讐.md`'s own aliases (`讎`) and `words/` for any variant — nothing exists under either form; **Created 2026-07-17**: `words/私讐.md` — "personal grudge," compositional CJKV (`sīchóu`/`si1 cau4`/`ししゅう`/`사수`/`tư thù`, the last three confirmed via Wiktionary/WebSearch, Mandarin/Cantonese derived compositionally since 讐/讎 is a literary-register character with no independent modern citation), documented as the `stand_in` legitimizer for [[讐]] per [[feedback_standin_note]]. Notes cover the classical private/public-wrong distinction (公報私仇) and the real-world register split where modern Mandarin/Cantonese default to 仇 instead — confirmed 仇 is **not** a graphemic variant of 讐/讎 in this vault (it's already its own independent character, [[仇]], `注音 ㄍ⼜`, with its own separate stand-in [[仇恨]]), so it was documented in prose rather than added as a frontmatter alias. Backlinked on both [[私]] and [[讐]]'s own `## Words` sections (the latter had no `## Words` section at all before this); `ㄙㄨ` syllable page's citation completed with its ruby and restamped `2026-07-17` (this was the page's last remaining gap). Word-creation backlog now at 6 remaining (熨斗, 狡猾, 边疆, 島嶼, 兄嫂, 梯子); `私讐`/`鬚髯` both resolved (one via creation, one via a stand_in-typo fix).

**Created 2026-07-17**: `words/呪文.md` — "incantation, spell," the string of words itself as opposed to the practice of spellcasting. CJKV: `zhòuwén`/`zau3 man4`/`じゅもん`/`주문` (Japanese/Korean confirmed via Wiktionary/WebSearch; Mandarin/Cantonese derived compositionally, cross-checked against the attested modern-Chinese form `咒文`). **Japanese じゅもん is a lexicalized reading, not the plain on'yomi concatenation** (呪 ju + 文 bun would give *jubun*) — same "fused reading" gotcha as calendar words like 八日, documented explicitly in Notes. `vietnamese` omitted: WebSearch confirmed 呪/咒 itself reads `chú` in Sino-Vietnamese, but no attested compound reading for 呪文 specifically turned up, so left blank per the omit-don't-fabricate rule rather than guessing "chú văn." Added `咒文` as an alias (咒 is already a documented true glyph-variant of 呪 on the character's own page, unlike the 仇/讐 case last time — safe to alias here). Documented as the `stand_in` legitimizer for [[呪]] (which had no `## Words` section at all before this). Backlinked on both [[呪]] and [[文]]. This was the last gap on the `ㄐㄨㄛ` syllable page (from the `2026-05-01`-batch audit) — now fully resolved and restamped `2026-07-17`, closing out that page for good.

**Created 2026-07-17**: `words/熨斗.md` — "flatiron." CJKV: `yùndǒu`/`wan6 dau2`/`のし`/`울두`/`uất đẩu` (Korean/Vietnamese confirmed via WebSearch, matching 熨's own `uất` and 斗's own `đẩu` compositionally; Cantonese `dau2` differs in tone from 斗's own recorded `dau3`, cited as attested for the compound rather than forced to the character-level tone). Etymology: 熨 "press with heat" + 斗 "ladle-shaped vessel" — the ancient flatiron was literally scoop-shaped, filled with hot coals, same 斗 as the Big Dipper's. **Real, striking semantic drift in Japanese**: modern Japanese doesn't use 熨斗 for the ironing appliance at all (that's the loanword アイロン) — の し (noshi) instead names the folded dried-abalone/paper strip attached to formal gifts, a related-but-distant ceremonial object, documented explicitly in Notes as a case where the shared character string carries genuinely different referents across the CJKV languages. **New character-vs-word `注音`-adjacent mismatch, same class as `恩`/`慣`/`調`·`酒`**: 熨's own vault Korean reading is `위`, but the real attested compound reads `울두`, using 熨's *other*, less common polyphonic reading `울` — flagged in Notes, not corrected (character-page verification is out of scope for a word-creation task). Documented as the `stand_in` legitimizer for [[熨]] (no `## Words` section existed on either constituent character before this). Backlinked on both [[熨]] and [[斗 (char)]]. This was the sole remaining gap on `⼜ㄊ` (also fixed a stray non-canonical intro line while there — "has no meaning when used alone" → the exact checklist template); page fully resolved and restamped `2026-07-17`. Word-creation backlog now at 5 remaining: 狡猾, 边疆, 島嶼, 兄嫂, 梯子.

**Created 2026-07-17, with a real fork resolved along the way**: `words/狡猾.md` — "cunning, crafty," a synonym-doubling compound (both halves independently mean the same thing). Before creating it, discovered `猾` has **no character page of its own** — it's recorded only as an alias on `characters/滑 (char).md` (滑, "slippery"). This looked at first like the same class of miscategorization as `㐮`/`攴`, since 猾's real meaning ("crafty") is completely unrelated to 滑's ("slippery"); flagged it to the user with a recommendation to split 猾 into its own page (the two are true Middle Chinese homophones from the same phonetic 骨, structurally the same relationship as this vault's own [[且]]/[[処]] pair, which *did* get split into two entries). **User's ruling, now a standing convention**: when a character is registered as an alias, the parent form is the real entry and the alias is never used independently — so 猾 stays folded into 滑, and the word's own `characters:` frontmatter lists `滑 (char)`, not a (nonexistent) `猾` page, even though the word is conventionally written/read as 狡猾. CJKV all compositionally clean, no fused readings: `jiǎohuá`/`gaau2 waat6`/`こうかつ`/`교활`/`giảo hoạt`, all confirmed via Wiktionary. Documented as the `stand_in` legitimizer for [[狡]] (which had no `## Words` section before this); backlinked on both [[狡]] and [[滑 (char)]]; the graphemic curiosity (猾/滑 homophone-but-merged, contrasted with 且/処 homophone-but-split) is written up in the word's own Notes rather than silently resolved. This was the last gap on `ㄍ⼄ㄨ` — restamped `2026-07-17`. Word-creation backlog now at 4 remaining: 边疆, 島嶼, 兄嫂, 梯子.

**Created 2026-07-17**: `words/边疆.md` — "frontier, border region." Before writing it, discovered neither `边` nor `邊` exists as its own character page — but this time, unlike `猾`/`滑`, the vault already has the right home: **`辺`** (Japanese shinjitai, already a full character page with several existing words) has `邊` and `边` listed on its own `aliases:` field. Applying the standing rule from the `狡猾` case directly this time, no need to re-ask: `辺` is the real entry, so the word's `characters:` frontmatter lists `辺`, not a nonexistent `边`/`邊` page — while the word itself keeps the conventional spelling `边疆` (matching `疆`'s own `stand_in`). CJKV: `biānjiāng`/`bin1 goeng1`/`へんきょう`/`변강`/`biên cương`, all clean compositional concatenations of the two characters' own readings, all confirmed via Wiktionary/WebSearch. **Real find in the process**: Japanese actually preserves 辺疆 (henkyō) as the *historically original* form of the everyday word [[辺境]] (already in the vault) — 境 was substituted for 疆 because 疆 isn't a Jōyō character, the same kanji-substitution pattern as 罔/両 standing in for 魍/魎 elsewhere in this vault. **Also flagged, not fixed**: `辺.md`'s own stored `cantonese` field is `dou6`, which doesn't correspond to any real reading of 邊/边/辺 — a plain data error, independent of anything in this task; the word's own `cantonese` field uses the Wiktionary-confirmed `bin1 goeng1` instead, and the discrepancy is written up explicitly in the word's Notes rather than silently inherited or silently fixed on `辺.md`. Documented as the `stand_in` legitimizer for [[疆]] (no `## Words` section existed there before this); backlinked on both [[辺]] and [[疆]]. Last gap on `ㄍ⼘ㄋ` — restamped `2026-07-17`. Word-creation backlog now at 3 remaining: 島嶼, 兄嫂, 梯子.

**Correction (2026-07-17, caught by the user): `島嶼` was never actually missing either** — same exact bug shape as `鬚髥`/`鬚髯`. `words/島屿.md` already exists (dated `2026-05-23`, fully complete, ruby `ㄊㄚㄨㄙ⼄`) and already lists `島嶼` on its own `aliases:` field; `屿.md`'s own `## Words` section even already pointed at `[[../words/島屿]]` correctly. The bug was in the same place as last time: `屿.md`'s own `stand_in` field said `島嶼` (the alias form) instead of `島屿` (the real filename) — fixed the `stand_in` field, the `ㄙ⼄` syllable-page citation (now properly ruby'd and restamped), and a stray self-reference to `島嶼` inside `島屿.md`'s own prose. **This is now confirmed as a recurring, not one-off, failure mode in my own duplicate-checking**: my original backlog-flagging pass searched only for the exact `stand_in` string cited on the character page, not for an alias-swapped variant of that string. Going forward, before flagging any `stand_in` compound as missing, check the constituent character's own `aliases:` field for each component and search `words/` under every combination, not just the literal `stand_in` string. Word-creation backlog now at 2 remaining: 兄嫂, 梯子 — pre-checked both against this same failure mode immediately: 兄(char)/嫂 and 梯/子 all have empty `aliases:` fields, and no `words/` file exists under any of their names or components' aliases — both confirmed as genuine gaps, not repeats of the `鬚髥`/`島嶼` pattern.

**Created 2026-07-17**: `words/兄嫂.md` — "sister-in-law, older brother's wife," mildly redundant by design (嫂 alone already carries this exact meaning; 兄 makes the relationship fully explicit). CJKV: `xiōngsǎo`/`hing1 sou2`/`あによめ`/`형수`/`huynh tẩu`, all confirmed via Wiktionary/WebSearch. **Japanese has a real register split worth noting**: あによめ (native, everyday) vs. けいそう (Sino-Japanese, formal/classical) — used the common native reading in frontmatter, documented the Sino-Japanese alternate in Notes. `嫂.md` itself turned out to already be fully perfected on every other criterion (all 4 Notes bullets present) — creating this word was the only thing blocking its own `date-last-perfect`, so stamped it too. Documented as the `stand_in` legitimizer for [[嫂]]; backlinked on both [[兄 (char)]] and [[嫂]]. Last gap on `ㄙㄚㄨ` — restamped `2026-07-17`. Word-creation backlog now at 1 remaining: 梯子.

**Created 2026-07-17**: `words/梯子.md` — "ladder," the last item on this backlog. 子 here is its extremely productive light noun-suffix role (as in 椅子, 帽子, 扇子 — already a huge, loosely-organized list on [[子]]'s own page), not "child." **Japanese has a fixed native reading はしご (hashigo)**, not the plain on'yomi concatenation (ていし survives as a secondary, more formal reading) — same "dominant native reading over concatenation" pattern as 呪文's じゅもん. **Neither Korean nor Vietnamese has any real Sino-form life for this word** — both languages use a native, unrelated word for "ladder" (사다리, *thang*) instead. `vietnamese` omitted per the no-attestation rule; `korean` (a required field, no omit option) uses the mechanical concatenation 제자, explicitly documented in Notes as non-attested rather than presented as real — and flagged as a pure homophone collision with the unrelated word 弟子 ("disciple"), also read 제자. Documented as the `stand_in` legitimizer for [[梯]]; backlinked on both [[梯]] and [[子]] (added under 子's existing "Thing" grouping, alongside 椅子). Last gap on `ㄊㄝ` — restamped `2026-07-17`.

**Word-creation backlog fully cleared.** All 9 compounds ever flagged across this session (熨斗, 狡猾, 边疆, 呪文, 島嶼→島屿, 兄嫂, 梯子, 私讐, plus the false-alarm 鬚髥→鬚髯) are now resolved — 7 created, 2 fixed as stand_in-typo corrections. Ground-truth triage remains clean at 0/894 mismatches across the whole syllable corpus.

- **`2026-05-02`-dated batch, full checklist audit (2026-07-17)**: same house-style era as `2026-05-01`, confirmed by a quick mechanical pre-scan — 16 of 25 files missing `### Common`/`### Advanced`/`### Naming` structure and the full three-script intro citation. All 25 reviewed; 24 fully fixed and restamped `2026-07-17`. **New defect variants found in this batch, beyond the 05-01 signature**: a genuinely truncated intro (`ㄍㄧㄊ`'s read literally `**ㄍㄧㄊ** means` with nothing after "means"); a truncated bound-entry citation with the compound name entirely missing (`ㄎ⼄ㄎ`'s 曲 entry read `"bend" but requires ` with a trailing space and nothing else); a truncated `但 requires` with no `名専字` (none this batch, but a related bug: `ㄏ⼔ㄫ`'s 宏 entry used the invented non-canonical phrase `"vast, grand" — naming character` instead of either real template); missing rubies on otherwise-correct bound entries (`ㄅㄨㄫ`'s 朋→朋友, `ㄎㄧㄋ`'s 巾→手巾, both words already existed, just needed the `<ruby>` tag added); the stand-alone `-->` arrow used on bound characters (`ㄍㄛ`'s 故, `ㄙㄚ`'s 寺, `ㄙㄧㄎ`'s 飾 — three more instances of the exact bug class already found in `ㄙㄚㄫ`/`ㄙㄝㄎ` during the 05-01 audit); an unclosed `dataview` fence plus a non-canonical `諺文`-filtered query with a dead OR-clause (`ㄌㄝㄫ`, which also had a reversed 諺文/羅馬字 script order in its intro, a curly-brace gloss `{causative}` instead of quotes, a `requies` typo, and two `"but name only"` entries that should have read `但 it's a 名専字`). **1 left unstamped, gap flagged**: `ㄏ⼔ㄫ`'s 宏→`宏大` — no `words/宏大.md` exists; added the entry without a ruby, a 10th item for the word-creation backlog (previously declared fully cleared — it wasn't quite, this batch reopened it by one). Ground-truth triage re-run clean after (0/894 mismatches).

**Created 2026-07-17**: `words/宏大.md` — "grand, magnificent, vast." Near-synonym doubling, same shape as 狡猾: 宏's own `korean_native` gloss (클, "big") is identical to 大's, the same character-level tell already seen for other synonym-pair compounds. CJKV: `hóngdà`/`wang4 daai6`/`こうだい`/`굉대`/`hoành đại`, all clean compositional concatenations, confirmed via Wiktionary/WebSearch. **Japanese こうだい is real but comparatively rare** — Wiktionary records 宏大 as an alternative spelling of the far more common 広大 (also こうだい, using 広/廣 instead of 宏 — a different character, not a variant of it, just semantically overlapping); the reading itself is unaffected by the choice. Documented as the `stand_in` legitimizer for [[宏]] (which was otherwise already a fully-perfected character page — this word was the only thing standing between it and its own completion, though its `date-last-perfect` was already set from 2026-05-13, so no restamp needed there). Backlinked on both [[宏]] and [[大 (char)]] (the latter's `## Words` section is now enormous, same growth pattern the user flagged for [[子]]). Last gap on `ㄏ⼔ㄫ` — restamped `2026-07-17`. **Word-creation backlog fully cleared again**, 10/10 resolved.

- **`2026-05-03`-dated batch, full checklist audit (2026-07-17)**: the biggest single-day batch so far (51 files), and a genuinely different profile from `05-01`/`05-02` — only 26 of 51 missing the `### Common`/full-intro structure (about half, not ~80%), confirmed by the same mechanical pre-scan before committing to the full sweep. Fixed all 26 structurally-broken files (same defect shapes as before: bare `lacks meaning`/`lacks intrinsic meaning` phrasing instead of the exact template, missing rubies on already-correct bound entries, a couple of genuinely truncated intros — `ㄍ⼶ㄧ`'s read `**ㄍ⼶ㄧ**  means ` with nothing after it — and one link pointing at itself twice, `ㄍ⼘ㄎ`'s 脚 entry arrowed to its own character page instead of `words/脚.md`). Then audited the 22 pages that already had subsection structure, and found the **stand-alone `-->` arrow misused on bound characters was the single most common defect in this batch** — 8 separate instances across 5 files (`ㄍㄛㄨ`'s 垢, `ㄙㄛ`'s 遡/蘇/鞘/藷, `ㄙㄝㄊ`'s 洩/楔, on top of the 3 already found in `05-01`/`05-02`), every one already carrying a correct ruby, just the wrong connective phrase. Also found 3 more missing rubies on already-correct bound entries (`ㄇㄚㄎ`'s 麦/漠/膜 → 大麦/沙漠/薄膜, all three word files already existed) and one more unnumbered, wrong-arrow, phantom-character entry (`ㄐㄝㄎ`'s 瘠 — no character file exists anywhere, same class as `攴`/`㐮`/`鷲`; removed, not fabricated). **One new corpus gap surfaced, different in kind from the others**: `ㄍㄨ`'s 衢 has `stand_in: 衢` (i.e. it's supposed to be *stand-alone*, not bound to a compound) but no `words/衢.md` exists — this is the checklist's own explicitly-named case ("a character's `stand_in` points to itself but no matching word file exists"), left unstamped and un-fabricated rather than treated as a bound-word gap. 48 of 51 files fully fixed and restamped `2026-07-17`; `ㄍㄨ` stays on its original stamp pending that gap. Ground-truth triage re-run clean (0/894 mismatches).

**Resolved 2026-07-17**: the user renamed `characters/衢.md` → `characters/衢 (char).md` (the vault's own signal that a same-named word file is about to exist) and asked for the word. Created `words/衢.md` — single-character stand-alone word, same minimal template as `且.md`/other single-character words (`characters:` list with one entry). CJKV all straight from the character's own frontmatter (already Wiktionary-sourced): `qú`/`keoi4`/`みち`/`구`/`cù`. Notes cover 衢's specific sense (a road that *branches*, not a road in general) and the classical numbered-fork idiom (三衢, 九衢). While in there: added the character page's missing disambiguation callout (a word now exists at the same name), fixed its `kwin` (was `false`, but `諺文`/`korean` are both byte-identical `구` — should be `true`), and set its `date-last-perfect` (all 4 Notes bullets were already correct; the missing word was the only blocker). Also fixed a malformed link left on the `ㄍㄨ` syllable page itself (a relative path had gotten pasted inside wiki-link brackets — `[[../characters/衢 (char)]]`, which doesn't resolve) and restamped it. Ground-truth triage remains clean (0/894).

- **`2026-05-06`-dated batch, full checklist audit (2026-07-17)**: small batch (16 files), only 4 missing the `### Common` structure — fixed all 4 (same defect shapes as before: bare phrasing instead of the exact no-meaning template, missing three-script intros, one truncated-then-rebuilt entry). Audited the other 12 already-structured pages via the stand_in-consistency script; found only 2 real issues, both missing rubies on otherwise-correct bound entries (`ㄏㄝㄋ`'s 顕→顕著). **One new, distinct defect class surfaced**: `ㄙㄨㄛ`'s 収 entry cited `収集` (a real, existing word that does use 収) — but 収's own `stand_in` field actually says `回収`, a *different* compound that has no `words/` file at all. This isn't the usual "missing ruby" gap; the syllable page had been quietly substituted with a different, unrelated-but-valid compound instead of the character's actual designated stand-in, papering over the real gap. Per the same standard applied to `鬚`/`島嶼` earlier, corrected the citation back to `回収` (matching `stand_in` exactly) and left it unruby'd/unstamped as a genuine word-creation gap, rather than accepting the substitute as if it resolved anything — even though 収 is independently well-attested elsewhere (収入/収穫/収蔵/収拾, already listed loose under 収's own page, itself missing a proper `## Words` heading, not fixed here — out of scope). 15 of 16 files fully fixed and restamped `2026-07-17`; `ㄙㄨㄛ` stays on its original stamp. Word-creation backlog reopened at 1: **回収**. Ground-truth triage re-run clean (0/894 mismatches).

**Created 2026-07-17**: `words/回収.md` — "recover, retrieve, collect back, recycle." CJKV: `huíshōu` (HSK 5)/`wui4 sau1`/`かいしゅう`/`회수`/`hồi thu`, all clean compositional concatenations (Japanese/Korean/Mandarin directly confirmed via Wiktionary/WebSearch, Cantonese/Vietnamese derived compositionally from each character's own field). **Vietnamese colloquially prefers the reversed order** *thu hồi* to the compositional *hồi thu* used here — a real, documented Sino-Vietnamese reordering pattern, noted in the word's Notes. Also noted: 収 is far from a quiet single-use character despite needing 回収 as its official stand-in — it already has real independent life via 収入/収穫/収蔵/収拾, all pre-existing on its own page. While there: added `収.md`'s missing `## Words` heading (its four existing word entries had been floating as bare bullets with no section header at all — a separate, unrelated defect noticed in passing, fixed alongside the backlink). Documented as the `stand_in` legitimizer for [[収]]; backlinked on both [[回 (char)]] and [[収]]. Last gap on `ㄙㄨㄛ` — restamped `2026-07-17`. **Word-creation backlog fully cleared again.**

- **`2026-05-07`-dated batch, first half (2026-07-17)**: the largest batch by far (92 files) — user asked for a preview scan before committing, which paid off: only **9 of 92** missing the `### Common` structure, the cleanest ratio of any batch swept so far. Fixed all 9 (same defect shapes, plus one genuinely new one: `ㄑ⺢`'s intro referenced a completely different syllable code, `**ㄑㄚ** means "car"`, apparently copy-pasted wholesale from an unrelated page — rebuilt using this page's own `羅馬字`/`諺文`). Two more phantom-character entries found and removed (same class as `攴`/`㐮`/`鷲`/`瘠`): `ㄆ⼶`'s 俾 — no character file exists anywhere. Built and ran an **automated stand_in/arrow/ruby consistency checker** (rather than manually reading every file) across the first 37 of the 83 already-structured pages — found only 4 real issues, all missing rubies on otherwise-correct bound entries (`⺢ㄫ`'s 旺→興旺, `ㄅㄛㄋ`'s 坂→斜坂, `ㄆㄚㄨ`'s 泡→泡沫 and 疱→面疱), fixed and verified against each word file. All 46 files processed this round (9 rebuilt + 37 audited) restamped `2026-07-17`. **46 files remain** in this batch for a follow-up pass (the rest of the already-structured 83, i.e. positions 38–83 alphabetically). Ground-truth triage re-run clean (0/894 mismatches). **Lesson for the rest of the backlog**: before creating any of the other flagged compounds, check the constituent character's own `aliases:` field and `words/` for variant-character forms first — a `stand_in` citing a rare/alias glyph instead of the canonical one can look exactly like a missing word when it's actually a one-field typo. Cross-checked 3 files that already had partial structure (`ㄏㄚㄋ`, `ㄐㄛㄨ`, `ㄙ⼄ㄨ`) end-to-end against every entry's `stand_in` — all correct, restamped as a genuine re-verification (no content changes needed), same treatment `Stroke 04` got in the original Stroke lint. Ground-truth triage re-run clean afterward (0 mismatches across all 894 codes) — this was a pure qualitative-defect pass, no count drift involved.
  - **Original March/April sweep (2026-07-09, superseded by the above)**: wrote the `lint Syllables` procedure into [[AIOS/skills/skill_lint.md|skill_lint.md]] (grounded in [[AIOS/checklists/checklist_syllables.md|checklist_syllables.md]]), mirroring the SKIP/Radicals structure, then substantially hardened it over ~5 refinement rounds as batches surfaced new defect classes — this is the single most-iterated procedure in the file and worth re-reading in full before resuming, not just skimming from memory. Working oldest-first, `date-last-perfect` by month: March (28 files, 03-29 through 03-31) and April (119 files, 04-01 through 04-30) were fully swept — 147 of 895 syllable pages done at the time, 143 stamped `2026-07-09`, 5 correctly left unstamped pending open corpus decisions (below).
  - **Procedure hardening, the load-bearing parts**: quote-tolerant ground-truth grep (`注音: "X"` vs `注音: X` both exist in `characters/`, a naive exact grep silently dropped ground truth for entire syllables); `grade_level` (subsection) and `stand_in` (entry format: stand-alone/bound/名専字) are fully independent axes — the single most-recurring mistake; primary-meaning rule covers every gloss on a page (frontmatter, intro, and each entry's own description), including stripping parenthetical qualifiers, without ever inventing outside knowledge past what's in the character's own `english` list; ruby text must match a compound's own stored `注音` byte-for-byte (not just "has a dot"); numbering continuous across all three subsections, sorted by `grade_level` ascending within each; audio lines keep their leading slash (that's canonical, not a hygiene bug) but must be checked to actually exist on disk; several distinct intro-block failure modes (truncated, romanization-substituting-for-bopomofo, wrong-syllable copy-paste, self-contradicting romanization stated twice); a page can reference characters that don't exist anywhere in the corpus, not just ones misfiled elsewhere — same "flag, don't fabricate, don't stamp" treatment as any other gap.
  - **Corpus-integrity bug class discovered mid-sweep**: a character's own `注音` frontmatter (correct, and why it's ground truth for its syllable page) can disagree with its corresponding word file's `注音` — invisible via ruby since stand-alone entries carry none. Found and fixed 3 times so far, all confirmed character-side-correct by the user: 屎 (word had ㄏㄧㄜ, fixed to ㄏㄜ), 破 (word had ㄆㄚ, fixed to ㄆㄜ — its 羅馬字/諺文 already matched ㄆㄜ, pure copy-paste typo in one field), 即 (word had ㄐㄧㄎ, fixed to ㄐㄜㄎ). Watch for more.
  - **Other corpus fixes made along the way**: 怯 (on `ㄎㄚㄆ`) had `stand_in` pointing to itself but no word file — per user decision, created [[words/怯]] and renamed the character file to `怯 (char).md` (it was previously bare `怯.md`, colliding with the word-file naming slot), then fixed all 7 vault-wide backlinks that pointed at the old bare filename (lookup pages under SKIP/Radicals/HSK/Japanese/Korean). 暈's `stand_in` typo (眩暈 → 玄暈, the word file that actually exists). Two long-stamped `ㄅㄧㄆ`/`ㄍㄝㄆ` pages had `羅馬字` typos (bip/gep) breaking the confirmed -ㄆ→-b convention, fixed to bib/geb.
  - **Open corpus decisions — all 5 now resolved (2026-07-09), done one at a time at the user's request:**
    - `ㄌㄜㄇ`: ground truth was genuinely empty (no character anywhere has `注音: ㄌㄜㄇ`) but the page listed 3 fully-formed entries. Resolved per-candidate: **廩** dropped (redundant with existing 倉 "granary," not in HSK/Jōyō, its only proposed word 倉廩 doesn't exist). **莻** — user's hypothesis was "check if it's an alias of 林"; first pass mistakenly researched the visually-similar but wrong character 莻(U+83BB, Korean-only "late"), corrected and re-researched the actual 菻(U+83FB); turned out to literally use 林 as its phonetic component, so added as an alias on [[林 (char)]] instead of creating a separate page. **懍** ("fear, awe") created as a full character — its correct MC derivation (力稔切, l+侵) actually matches 林/淋's existing ㄌㄧㄇ, not ㄌㄜㄇ, but the user deliberately chose to force it onto ㄌㄜㄇ anyway to keep the syllable non-empty (documented as an explicit exception in the character's own Notes; see [[feedback_syllable_placement_override]]). Page now has just 懍, stamped.
    - The remaining 4 were all "create the missing stand-in compound word" gaps, done one at a time via `skill_word_creation.md`: **凌駕** (凌, `ㄌㄜㄫ`), **欠乏** (乏, `ㄅㄚㄆ` — cross-linguistic note: 欠乏 is Japan's default term but China/Korea prefer the 缺-based near-synonym 缺乏/결핍 instead), **汚穢** (穢, `ㄝ`), **堡塁** (堡, `ㄅㄚㄨ`). All four: full CJKV frontmatter sourced from Wiktionary/WebSearch with compositional derivation as the tiebreaker (concatenate component characters' own 羅馬字/諺文/注音, don't trust a word-level source over that), encyclopedic Notes per [[feedback_notes_style]], stand-in clause per [[feedback_standin_note]], character backlinks + syllable-page ruby+stamp per [[feedback_syllable_completion]]. Along the way: confirmed the vowel-initial romanization/注音 concatenation convention (`'o'e`, `ㄛ·ㄝ`) against precedent; added `luỹ` to 塁's own `vietnamese:` field (previously blank) after confirming it independently via the compound; fixed a stray duplicate `## Notes` heading on `堡.md`. **Not fixed, flagged for the character-perfection sweep**: `characters/堡.md` is missing its SKIP/Stroke and Levels Notes bullets entirely (only has the graphemic one) and has `mc_id: 0` (probably should be blank) — left unstamped; its graphemic bullet also looks like semantic/phonetic may be swapped (labels 保 semantic with an empty gloss, 土 phonetic) but wasn't verified/fixed, out of scope for a word-creation task.
    - **New permanent rule discovered mid-task**: the `korean` field always uses North Korean/문화어 pronunciation (e.g. 림 not 임), never South Korean 두음법칙-shifted forms — see [[feedback_korean_reading_north]]. Confirmed already-consistent across the existing corpus; explains why `kwin` (諺文==korean check) so often comes out `true` for words with a word-initial ㄹ/ㄴ character. Directly relevant any time a word/character creation or lint pass touches `korean`/`諺文`/`kwin`.
  - Dispatch pattern that's working well: 3 forks of ~10 files per wave (parallel), each explicitly asked to flag new bug classes / procedure ambiguities, consolidated into a procedure rewrite between waves before the next wave launches against the updated text.
- **MediaWiki migration** (compare `danayo.conlang.org` legacy pages against vault `characters/`, see [[skill_mediawiki_migration]]): last character confirmed/fixed was 缸 (2026-06-20). Distinct from the sweep below — user distinguishes these explicitly; don't conflate them.
- **Character-page perfection sweep, `/loop` batch (2026-07-16, new thread)**: a 10-minute self-pacing `/loop` picking up the never-perfected pool directly — `grep -L "^date-last-perfect: [0-9]" characters/*.md` currently returns 3032 of 3350 character files (most of the corpus has never been touched by this checklist at all). Working ascending by `danayo_id` (most fundamental/common characters first) rather than the manual/oldest-date ordering the 2026-07-03 thread below used. Full log with per-character defect notes lives in [[Loop Work.md]], not duplicated here — check that file for the current stopping point rather than this line, since the loop updates it every iteration. First iteration (2026-07-16) did 小 (id 23) and 口 (id 24), both had never been touched: notes bullets mashed together/missing the `../` path prefix, several Words entries missing ruby or missing outright, one word-file `characters:` filename-form bug class (bare `口` instead of `口 (char)`, found on 3 files), and two incidental content bugs fixed along the way (a word's English gloss typo, a Cantonese field with pinyin copy-pasted into it, one flatly wrong gloss unrelated to the character itself). Confirmed the `graphemic_classification` field is the right cheap check for whether a `## Derived Characters` section applies (grep for other characters naming this one as their phonetic component) rather than trying to enumerate every character that merely shares the radical.
- **Character-page perfection sweep, Components-driven thread (2026-07-17, new thread)**: per user request, prioritizing the 152 characters listed as "in" targets under `## Components` in [[broken links output.md]] — each line there is a broken-link component (e.g. `[[卩]]`, `[[丨]]`) followed by the vault characters that reference it in their own graphemic bullet. The user's insight: many of these "missing" components aren't missing characters at all, just Kangxi radicals that were bare-linked instead of pointed at their `Lookup/Radicals/Radical NNN.md` page — running the standard perfection checklist (which already has the radical-linking rule, see `checklist_characters.md` bullet 1) on the *consuming* character resolves the false-positive broken link as a side effect, without needing to create a character page for something that was never a character. Distinct ordering criterion from both threads below — don't conflate "last one done" pointers. Full log lives in [[Loop Work.md]] under its own thread heading. Done so far (2026-07-17): 光 (fixed bare `[[卩]]`/`[[火]]` component links to `[[Radical 026|卩]]`/`[[Radical 086|火]]`, rebuilt Words 6→11 entries + added missing Chengyu section + Derived Characters 恍/洸/晃/胱), 即 (same 卩 fix, reordered Words before Chengyu, added missing 即/即日 words + 一触即発 chengyu + Derived Characters 節, split a merged SKIP+MC bullet into the canonical two), 皮 (`graphemic_classification` was the bare tag `為` — a plain data error, no such classification exists — corrected to `象形` per Wiktionary and rewrote the bullet from stale "Components:" text; added missing `>[!tip]` callout since it had none; Words 2→9 entries; added Derived Characters for the well-attested 彼/披/破/被/頗/波/玻/疲 phonetic series, each gloss pulled from the character's own `english:` field rather than guessed), 色 (same 卩 fix, added missing callout + Levels bullet, Words 19→32 entries — largest gap-fill this thread so far — Derived Characters 赩), 見 (merged a duplicate `## Notes` heading, fixed a `graphemic_classification: 指事` vs. body-text `会意` contradiction, built Words/Chengyu/Derived Characters from scratch — Derived Characters 現/硯), 刈 (the broken link `[[㐅]]` was a rare variant glyph of 乂, which already has its own perfected character page in this vault — repointed the bullet at the real target instead of creating anything new; also found and fixed a genuine gap in `checklist_characters.md`'s Level-mapping table, `grade_level: 先進` → `[[Grade Advanced]]`, undocumented until now), 千 (user-flagged as a real etymology error, not just a broken link — `graphemic_classification: 遷` and the bullet both wrongly claimed phonetic 遷; Wiktionary confirms the real phonetic component is 人, an extra stroke added to distinguish the numeral, independently corroborated by [[characters/年 (char)|年]]'s own already-perfected bullet — corrected the field and bullet, added the missing `## Derived Characters` (年); also fixed 5 Chengyu entries missing ruby/rt), 卑 (its broken `[[𠂇]]` link is a genuinely unlinkable no-page component, same class as the earlier 気/气 and 左/𠂇 precedents — described in prose instead of wiki-linked; existing 会意 etymology confirmed correct via Wiktionary, no fix needed there; promoted a wrongly-nested `### Descendants` to `## Derived Characters` and expanded 1→6 real hits, a well-attested p/b-initial phonetic family). 144 characters remain on the list; recommend running the rest via `/loop` given the scale, same pacing pattern as the `danayo_id` thread.
- **Character-page perfection sweep, manual thread (2026-07-03)**: going through existing `characters/` files one at a time and bringing each to full `date-last-perfect` compliance against [[AIOS/checklists/checklist_characters.md|checklist_characters.md]]'s 10-point checklist (frontmatter completeness, all four Notes bullets in order, complete Words/Chengyu sections with ruby+gloss) — independent of the wiki, no wiki lookup involved. Distinct from the `/loop` thread above (different ordering, different session) — don't conflate the two "last one done" pointers. Last one done: 仰 (fixed blank `pos`, missing Notes bullets 2-4, unformatted Words entry; phonetic component 卬 has no character page and isn't a radical, so — per user, don't create new characters to unblock another unless there's no other way — it was aliased onto [[卯]] instead, backed by Wiktionary's Etymology 4 for 卬 as "variant of 卯"; removed a conflicting pre-existing `卬` alias claim that had been sitting on 仰's own page). Before that: 代 (blank `pos`, wrong `graphemic_classification`, missing Notes bullets 2-4, several missing/unformatted Words+Chengyu entries; phonetic component 弋 *is* a radical, so was linked via its Radical page). Note: 昂 and 迎 also reference `[[卬]]` as their phonetic component and remain unperfected — now resolvable via 卯's alias, but not yet done. Also done: 保 (graphemic bullet was leftover wiki-style "Components:" text, not proper 会意 format; `### Derived Characters` was wrongly nested as H3 inside Notes instead of its own `##` section; `褓` was miscategorized as an alias — same category error as 卬/仰 — moved to Derived Characters, left unresolved since 褓 has no page; `## Words` section didn't exist at all, just 4 bare bullets missing 3 real words and the stand-in itself). Also done: 俯 (graphemic bullet was leftover wiki-style "Components:" text; needed a new supporting character 瞰 created — no CC/HSK presence, phonology borrowed from 敢/嵌 analogs — plus the new word 俯瞰 itself, since neither existed; added 瞰 as a 6th entry on the ㄎㄚㄇ syllable page). Note: that syllable page's `date-last-perfect` was already set despite its own entry 5 (坎) being an unresolved link to a nonexistent character — pre-existing bug, flagged but not fixed. Also done: 厄 (graphemic bullet was a bare "Components:" list; kept existing 指事 classification rather than switching to Shuowen's alternative 形声 reading; "Derived characters" was loose unheaded text, promoted to proper `## Derived Characters`; no `## Words` heading existed). Also done: 厠 (graphemic bullet was "Components:" text; 厂 here is a simplified stand-in for 廁's real semantic component 广 "building/shelter," noted explicitly; radical linked correctly this time per the corrected BP rule). Resume with the next character the user names.

## Closed / historical

- **Radical lookup pages — full lint pass (2026-07-08)**: ran a scripted audit of all 214 `Lookup/Radicals/Radical NNN.md` pages against [[AIOS/checklists/checklist_radicals.md|checklist_radicals.md]], cross-checking each page's numbered list against a `characters/` corpus map built from `radical:` frontmatter (not just trusting the page's own `## Data check` query). Found and fixed: ~68 pages with an unclosed ` ```dataview ` fence (content silently missing a closing fence at EOF — likely from earlier passes that never closed it); heading-level violations on 001/037 (`# Strokes`/`## +N` instead of `## Strokes`/`### +N`), 011 (`## By Stroke` naming), and 186 (orphan `### +0`/`### +11` with no `## Strokes` parent); a cosmetic "(char)" leaking into the display gloss on 11 entries across 10 pages; and 12 pages (003, 006, 013, 129, 175, 180, 189, 194, 201, 203, 204, 209) that were still bare lists or one-liners needing the real encyclopedic-opening + `### Used` treatment. The old `Lookup/Radicals/Radical Problems.md` backlog tracker (now deleted) undercounted this — several of the "already done, prosaic style" pages it listed (006, 013, 129, 203, 204) were not actually in that state when re-checked. All 214 pages now carry `date-last-perfect: 2026-07-08`. The audit script lives only in the session scratchpad, not the vault — if this needs to run again, it should be rewritten (radical→corpus map, glyph-extraction across `<ruby>[X](path)`/`[[X]]`/`[[X|Y]]` link forms, unclosed-fence detection).
