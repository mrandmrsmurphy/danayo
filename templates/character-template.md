---
variants: "<%* let x = await tp.system.prompt('Variants (comma-separated)'); tR = x ?? '' %><%= tR %>"
mandarin: "<%* let x = await tp.system.prompt('Mandarin pronunciation'); tR = x ?? '' %><%= tR %>"
cantonese: "<%* let x = await tp.system.prompt('Cantonese pronunciation'); tR = x ?? '' %><%= tR %>"
korean_sound: "<%* let x = await tp.system.prompt('Korean sound'); tR = x ?? '' %><%= tR %>"
korean_meaning: "<%* let x = await tp.system.prompt('Korean meaning'); tR = x ?? '' %><%= tR %>"
japanese_on: "<%* let x = await tp.system.prompt('Japanese On readings (comma-separated)'); tR = x ?? '' %><%= tR %>"
japanese_nanori: "<%* let x = await tp.system.prompt('Japanese Nanori (comma-separated)'); tR = x ?? '' %><%= tR %>"
vietnamese: "<%* let x = await tp.system.prompt('Vietnamese readings (comma-separated)'); tR = x ?? '' %><%= tR %>"
middle_chinese_initial: "<%* let x = await tp.system.prompt('Middle Chinese initial'); tR = x ?? '' %><%= tR %>"
middle_chinese_final: "<%* let x = await tp.system.prompt('Middle Chinese final'); tR = x ?? '' %><%= tR %>"
stroke_count: "<%* let x = await tp.system.prompt('Stroke count'); tR = x ?? '' %><%= tR %>"
radical: "<%* let x = await tp.system.prompt('Radical'); tR = x ?? '' %><%= tR %>"
skip_number: "<%* let x = await tp.system.prompt('SKIP number (#-#-#)'); tR = x ?? '' %><%= tR %>"
grade_level: "<%* let x = await tp.system.prompt(\"Dan'a'yo grade (1–6, 先進字, 名字)\"); tR = x ?? '' %><%= tR %>"
pos: "<%* let x = await tp.system.prompt('Part of speech'); tR = x ?? '' %><%= tR %>"
english_meanings: "<%* let x = await tp.system.prompt('English meanings (comma-separated)'); tR = x ?? '' %><%= tR %>"
romanization: "<%* let x = await tp.system.prompt('羅馬字 spelling'); tR = x ?? '' %><%= tR %>"
hangul_spelling: "<%* let x = await tp.system.prompt('Hangul spelling'); tR = x ?? '' %><%= tR %>"
joyo_level: "<%* let x = await tp.system.prompt('常用漢字 level'); tR = x ?? '' %><%= tR %>"
hsk_level: "<%* let x = await tp.system.prompt('HSK level'); tR = x ?? '' %><%= tR %>"
hanmun_edu_level: "<%* let x = await tp.system.prompt('한문교육용기초한자 level'); tR = x ?? '' %><%= tR %>"
danayo_id: "<%* let x = await tp.system.prompt(\"Dan'a'yo ID number\"); tR = x ?? '' %><%= tR %>"
mc_id: "<%* let x = await tp.system.prompt('MC ID number'); tR = x ?? '' %><%= tR %>"
graphemic_classification: "<%* let x = await tp.system.prompt('Graphemic classification'); tR = x ?? '' %><%= tR %>"
stand_in: "<%* let x = await tp.system.prompt('Stand-in (2-char word if not a word)'); tR = x ?? '' %><%= tR %>"
---
