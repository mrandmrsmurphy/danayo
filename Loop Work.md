# Loop Work

### 2026-07-16, iteration 6 — [[characters/手 (char)|手]] and [[characters/牛 (char)|牛]]

**[[characters/牛 (char)|牛]]**: blank `pos` → `名詞`. Sections were in the wrong order (`## Words` before `## Notes`) and Notes mixed floating CC links with 4 more Words-shaped entries (牛乳, 牛虻, 牛郎星, 牛郎) instead of an actual graphemic bullet — consolidated everything into the canonical structure and wrote the missing graphemic bullet (象形, ox-head pictograph). **Frontmatter bug fixed**: `middle_chinese_initial` was stored as plain `n`, but the character's own MC bullet already correctly pointed at the `聲疑` initial page — checked that page and confirmed its real IPA value is `ŋ` (velar nasal, matching Mandarin `niú`/Cantonese `ngau4`), so corrected the frontmatter field from `n` to `ŋ` rather than just papering over the mismatch in the bullet text. Words cross-check (9 total, one via the bare-"牛" bug) found 3 missing (水牛, 海牛, 蝸牛). Chengyu was entirely absent — added both hits (呉牛喘月, 対牛弾琴). No `graphemic_classification: 牛` hits — no Derived Characters. **Corpus bug fixed**: `words/蝸牛.md` had `characters: - 牛` instead of `- 牛 (char)`.

Next: 午, 文, 木, 王, 月 per `danayo_id`.

### 2026-07-16, iteration 7 — [[characters/午|午]] and [[characters/文|文]]

Next two by `danayo_id` (40, 41). Both stamped `date-last-perfect: 2026-07-16`. Neither needed a disambiguation callout (no `words/午.md` or `words/文.md`).

**[[characters/午|午]]**: blank `pos` → `名詞`. Notes had wrong heading level (`# Notes`), floating CC links, no real graphemic bullet (just 2 word-shaped entries) — researched via Wiktionary and wrote the graphemic bullet from scratch (象形, depicts a pestle, original form of 杵). Words cross-check (6 total) found 3 missing including the stand-in itself (正午, 午後, 午飯). **Caught two more wrong-ruby near-misses this iteration**: first drafted `午前` and `午後` with the identical rt `ㄛㄏㄛㄨ` (copy-paste slip) and `午餐` with a guessed `ㄛㄘㄚㄋ` — re-checked each word file individually before finalizing and corrected to the real values (午前 → `ㄛㄐㄝㄋ`, 午餐 → `ㄛㄑㄚㄋ`). This makes three iterations running where a guessed-rather-than-checked ruby almost shipped — worth remembering that pattern-matching rubies by ear is unreliable in this corpus specifically because Dan'a'yo readings don't always compose predictably from their parts. No chengyu, no `graphemic_classification: 午` hits.

**[[characters/文|文]]**: graphemic bullet was leftover wiki-style "Components: 亠, 乂" text describing a *false* decomposition — checked Wiktionary and confirmed real etymology is 象形/指事 (a man with a tattooed/painted chest, original form of 紋), matching the corpus's own `graphemic_classification: 象形` — rewrote properly, no components listed. Added radical disambiguation line (文 is Radical 067 itself). Words cross-check (25 total) found 12 missing (中文, 天文, 天文学, 序文, 文字, 文学, 文学者, 文法, 文章, 漢文, 諺文, 釈珈文尼) and reformatted 7 existing bare/dash entries. Chengyu cross-check found 4 missing (文体並存, 文言現代, 文音共決, 重文軽武) alongside the 2 already present. New `## Derived Characters` (4 hits on `graphemic_classification: 文`: 紋, 蚊, 旻, 閔) — all four verified via Wiktionary as genuine 形声/phono-semantic derivatives despite 旻/閔's MC finals (ɣiɪn) looking superficially unrelated to 文's own (ɨun): both trace to an OC medial-r infix (*mrjɯn vs plain *mjɯn) that regularly produces this exact MC split, a different (and legitimate) mechanism from the flat mismatches that got 要/表/失 excluded in prior iterations. **Incidental fix**: `words/中文.md` (already `date-last-perfect: 2026-06-28`) was missing its `注音` field entirely — added `ㄐㄨㄫㄇㄨㄋ`, composed from 中's own reading + 文's, needed anyway for this page's ruby.

Next: 木, 王, 月, 日, 少 per `danayo_id`.

### 2026-07-16, iteration 8 — [[characters/木 (char)|木]] and [[characters/王 (char)|王]]

Next two by `danayo_id` (43, 46). Both stamped `date-last-perfect: 2026-07-16`.

**[[characters/木 (char)|木]]**: sections were badly interleaved (Chengyu section sitting between the one Notes bullet and a batch of floating CC links + more Words-shaped entries) — reordered into Notes/Words/Chengyu/Derived Characters. Added the missing radical line (Radical 075) and SKIP/Stroke/MC/Levels bullets. Words cross-check (22 total, combining correct "木 (char)" and 6 buggy bare-"木" hits) found 11 missing and fixed all 6 bare-filename bugs (木偶, 木星, 木版, 梨木, 草木, 藍木). `graphemic_classification: 木` hit 2 characters (沐, 集): 沐 (m+uk, exact match) included in new `## Derived Characters`; 集 initially excluded since it's 会意 of 隹+木 (birds gathering on a tree) with no phonetic role for 木 — **corrected 2026-07-17 per user**: semantic-only components still count per checklist, so 集 belongs in Derived Characters too (added). **Incidental fix**: `words/木偶.md` had `english: pupper` (typo → puppet).

**[[characters/王 (char)|王]]**: same interleaving problem (Derived Characters and Chengyu both sitting before Words, wrong heading levels) plus a live link bug — `[[../chengyu/尊王攘夷]]` used a relative-path prefix *inside* a wiki-link, which breaks Obsidian's name-based resolution (the fix is just to drop the prefix, not the link, per the checklist's own documented gotcha). Bigger finding: the existing `### Derived Characters` entry (全) had **no corpus support at all** — grepped `graphemic_classification: 王` corpus-wide and got zero hits, then checked `全 (char).md` directly and found its own classification is `會意`, not `王`, with a completely unrelated MC reading (d͡z+iuᴇn vs 王's ø+ʉɐŋ) — this wasn't a defensible edge case like 友/内, it was an unsupported claim with no corroborating signal anywhere; removed it rather than perpetuating it. Radical line added (王's own `radical:` field is 玉, not itself, so it links to Radical 096, not a self-radical page like the last several characters in this sweep). Words cross-check (16 total, 1 via the bare-"王" bug) found 9 missing. **Caught two more guessed-not-checked rubies before finalizing** (same failure mode as the last two iterations): drafted 王冠 as `⺢ㄫㄍㄛㄋ` and 王妃 as `⺢ㄫㄆㄧ` by pattern, then verified against the actual word files and corrected to `⺢ㄫㄍ⺢ㄋ` / `⺢ㄫㄈㄧ`. **Corpus bug fixed**: `words/国王.md` had `characters: - 王` instead of `- 王 (char)`.

Next: 月, 日, 少, 心, 出 per `danayo_id`.

### 2026-07-16, iteration 9 — [[characters/月 (char)|月]] and [[characters/日 (char)|日]]

Next two by `danayo_id` (47, 48) — by far the largest pages this sweep has hit yet (63 and 88 constituent words respectively, mostly calendar vocabulary: the 12 zodiac months, 12 classical poetic month names, and every day-1-through-31-of-the-month word for 日). Both stamped `date-last-perfect: 2026-07-16`. Switched to a Python script pulling `注音`/`english` for a whole word list in one pass instead of one-by-one greps, given the volume — much faster and, critically, removes the guessing temptation that caused near-misses in recent iterations, since every ruby came verbatim from the script's own output.

**[[characters/月 (char)|月]]**: normalized an informally-worded callout, wrote the missing SKIP/Stroke/MC/Levels bullets, and replaced a placeholder `## Etymology` ("Pictogram/象形.") with a real graphemic bullet. Flattened a non-canonical `### Month` numbered sub-list (checklist has no subsection allowance under Words) into ordinary ruby+gloss bullets alongside everything else. Words cross-check (63 total, 5 via the bare-"月" bug) found the sweep's largest single gap-fill yet. Chengyu was missing 2 of 3 (鏡花水月, 日月星辰). `graphemic_classification: 月` hit only 絹 (char) — checked Wiktionary and found 絹's *real* phonetic component is 肙, not 月, and its MC reading (k+iuᴇn) shares nothing with 月's (ŋ+ʉɐt) — 月 has no relationship (phonetic *or* semantic) to 絹 at all, so this stays excluded as a genuine corpus error, unlike 表/失/集 (see 2026-07-17 correction on the 毛/手/木 entries: semantic-only components *do* belong in Derived Characters — this is a different, narrower case where the field is simply wrong). **Corpus bugs fixed**: 5 word files (日月, 明月, 昨月, 来月, 満月) had bare `- 月` instead of `- 月 (char)`; `日月.md` needed *both* characters fixed since it also had bare `- 日`; `chengyu/日月星辰.md` had the same bare `日`+`月` bug.

**[[characters/日 (char)|日]]**: same pattern at even larger scale — wrong heading level, no graphemic bullet (wrote one from scratch), 4 stray Words-shaped entries sitting in Notes, and a wiki-link with a `../` prefix baked in (`[[../words/棕枝主日]]`, the documented "prefix breaks Obsidian's name-based resolution" gotcha) — dropped the prefix rather than the link. Words cross-check (88 total, 5 via the bare-"日" bug) — while verifying I noticed **九日 wasn't in the ground-truth list at all despite the file existing**; traced it to a genuinely different bug class: `words/九日.md`'s `characters:` field was a single comma-joined scalar (`characters: 九 (char),日 (char)`) instead of a proper YAML list, so it silently fails any exact-match frontmatter query — fixed the YAML structure. `graphemic_classification: 日` hit 涅 — confirmed via Wiktionary as a genuine phonetic derivative (OC *njiɡ vs 日's *niɡ-type root) despite 涅's own MC final (et) looking different at first glance from 日's (iɪt), so kept it in Derived Characters. Chengyu cross-check found 2 missing (日月星辰, 一日三秋) alongside 日用必備. **Corpus bugs fixed**: 5 word files (日出, 日刊, 日報, 日記, 日語) had bare `- 日`; `chengyu/一日三秋.md` had the same bug. **Incidental fixes**: `words/日記.md` had `english: dairy` (typo → diary).

Next: 少, 心, 出, 市, 北 per `danayo_id`.

### 2026-07-16, iteration 10 — [[characters/少 (char)|少]] and [[characters/心 (char)|心]]

Next two by `danayo_id` (51, 52). Both stamped `date-last-perfect: 2026-07-16`. A calmer iteration after the huge 月/日 batch — both pages were already close to complete, mostly missing ruby/gloss formatting rather than whole sections.

**[[characters/少 (char)|少]]**: blank `pos` → `名詞`. All 7 ground-truth words were already listed (nothing missing) — just needed ruby+gloss added to 6 of the 7. Chengyu was missing both hits (多召少選, 少即是多) — added. `graphemic_classification: 少` hit 妙 (char) — verified via Wiktionary as a genuine phonetic derivative (OC root shared with 少, both from the "small/subtle" semantic-phonetic family that also produced 小→少→妙) despite the different surface initial (m- vs ɕ-) — included in new `## Derived Characters`.

**[[characters/心 (char)|心]]**: had the same "everything crammed into one bullet" defect as 小's very first iteration — SKIP/Stroke/syllable/CC links all merged into a single non-canonical bullet, levels bullet out of order — rebuilt into the standard 4 bullets. Notable: `joyo_level: "高等"` is the first character in this sweep to hit the non-default Jōyō tier — correctly mapped to `Jōyō - Kōtō`, not `Jōyō - Kyōiku`, confirming the mapping-table logic holds beyond the common case. Words cross-check (16 total, 1 via the bare-"心" bug on 点心) found 4 missing (中心, 心理学, 心臓, 灯心) and added ruby to 7 existing bare entries. Chengyu: all 4 ground-truth hits were already listed, just needed ruby/gloss on 2. **Caught two more guessed-gloss near-misses**: drafted 誠心誠意 and 邪心常悪's Chengyu glosses from vague memory of what the compounds "should" mean ("utterly sincere and devoted", "a wicked heart breeds constant evil") instead of reading their actual `english:` fields — re-checked before finalizing and corrected to the real stored glosses ("in all sincerity; with one's whole heart", "evil hearts, evil intents"). This is the same failure class as the guessed-ruby mistakes from earlier iterations, just applied to glosses instead of rt text — worth treating as a general rule: never write a ruby *or* a gloss from memory/inference, always read the source field, even when the meaning seems obvious. **Incidental fix**: `words/中心.md` (already `date-last-perfect: 2026-06-28`) was missing `注音` entirely, same gap class as `中文.md` last iteration — added `ㄐㄨㄫㄙㄧㄇ`, composed from 中's and 心's own readings (the word's own body text already spelled out this derivation, just never wrote it into frontmatter).

Next: 出, 市, 北, 目, 田 per `danayo_id`.

### 2026-07-16, iteration 11 — [[characters/出 (char)|出]] and [[characters/市|市]]

Next two by `danayo_id` (53, 54). Both stamped `date-last-perfect: 2026-07-16`.

**[[characters/出 (char)|出]]**: graphemic bullet (会意, 止+凵) was already correct — just needed reordering (floating CC links, `### Derived Characters` at H3 sitting before Words instead of after) and the missing SKIP/Stroke/MC/Levels bullets. Words cross-check (22 total, 6 via the bare-"出" bug) found 9 missing and reformatted several dash-gloss entries. Chengyu was entirely absent (引出奴家) — added. `graphemic_classification: 出` hit 拙 and 黜 — both confirmed via Wiktionary as genuine members of the well-attested 出/拙/黜 Old Chinese phonetic series (shared *-ud/-uds rimes despite surface initial differences t͡ɕ/t͡ɕʰ/ʈʰ), kept both. **Corpus bugs fixed**: 6 word files (傑出, 出奔, 出版, 出生, 出血, and the 出-side of 日出 which I'd only half-fixed in the 日 iteration — fixed its 日 side then, its 出 side now) had bare `- 出` instead of `- 出 (char)`.

**[[characters/市|市]]**: found and fixed a real frontmatter contradiction — `graphemic_classification` was stored as `象形`, but the page's own existing Notes bullet already read "形声 (OC *djɯʔ): semantic 兮 + phonetic 之," and Wiktionary confirms the 形声 analysis is correct — corrected the field to `之` (the phonetic component name) to match the content that was already there. **Bigger finding**: the frontmatter also listed `巿` as an alias of 市, and the page's old "Descendants" note pointed to 沛 — but 巿 (U+5DFF, "hem/knee-cover," OC *pub) and 市 (U+5E02, "city/market," OC *djɯʔ) are two entirely unrelated Kangxi characters that merely look nearly identical; Wiktionary confirms 沛's real phonetic parent is 巿, not 市, and the two characters share no phonetic relationship at all. Removed the false `巿` alias and did not carry 沛 into `市`'s Derived Characters (there are zero genuine hits for `graphemic_classification: 市` corpus-wide). **Flagged, not fixed**: 巿 has no character page anywhere in this vault, so this look-alike-conflation bug is currently invisible to any grep-based ground-truth check keyed on the character 巿 itself — worth a dedicated look whenever 巿-derived characters (沛 among them) come up in the sweep, since creating 巿's own page is out of scope for perfecting 市 today. Otherwise a small page: added SKIP/Stroke/Levels bullets, added the stand-in word (都市) plus 2 more missing words (城市, 胡志明市) to a Words section that previously just said "For 'market', use [[市場]]" in prose.

Next: 北, 目, 田, 本, 他 per `danayo_id`.

### 2026-07-16, iteration 12 — [[characters/北|北]], [[characters/目 (char)|目]], and [[characters/田|田]]

Three this time (55, 56, 57) since all were mid-sized rather than the previous iteration's huge calendar pages. All stamped `date-last-perfect: 2026-07-16`.

**[[characters/北|北]]**: blank `pos` → `名詞`. Graphemic bullet already good, just needed SKIP/Stroke/MC/Levels bullets. Words cross-check (16 total) found 10 missing including the stand-in (北方). **Caught two more guessed-ruby near-misses** (北端, 北面) — same failure mode as several prior iterations; verified both against their word files before finalizing and corrected (端: ㄉ⺢ㄋ not the guessed ⼔ㄚㄋ; 面: ㄇ⼶ㄋ not the guessed ㄇㄧㄝㄋ). No chengyu, no `graphemic_classification: 北` hits. **Incidental fix**: `words/北極熊.md` had `english: artic bear` (typo → arctic bear).

**[[characters/目 (char)|目]]**: smaller fix — added radical line (目 is Radical 109 itself), SKIP/Stroke/MC/Levels bullets, fixed a relative-path Words entry using a leading `/` instead of `../` (`[題目](/words/題目.md)`). Words cross-check (8 total) found 3 missing (品目, 盲目, 耳目). Chengyu (一目瞭然) was entirely absent — added. No `graphemic_classification: 目` hits. **Corpus bugs fixed**: `chengyu/一目瞭然.md` and `words/目宿.md` both had bare `- 目` instead of `- 目 (char)`. **Incidental fix**: `words/品目.md` had `english: artice` (typo → article).

**[[characters/田|田]]**: had a literal duplicate Words entry (田納素 listed twice, once as a normal bullet and again as a stray "abbreviation for tennessine" line) — consolidated to one. No callout needed (no `words/田.md`, stand_in is 田野 same pattern as 友/内/父/文/午/市). Words cross-check (7 total) found 4 missing including the stand-in (田野); added the missing graphemic bullet (象形, divided farmland plot) since Notes only had a bare CC-initial link before. Chengyu was missing 蒼海桑田 (only 李下瓜田 was listed). `graphemic_classification: 田` hit 佃 — exact MC match (d+en both), included in new `## Derived Characters`.

Next: 本, 他, 去, 右, 世 per `danayo_id`.

### 2026-07-16, iteration 13 — [[characters/本 (char)|本]] and [[characters/他 (char)|他]]

Next two by `danayo_id` (58, 59). Both stamped `date-last-perfect: 2026-07-16`.

**[[characters/本 (char)|本]]**: blank `pos` → `名詞`. Same duplicate-entry defect as 田's 田納素 last iteration — 日本素 was listed twice, once as a normal Words bullet and again as a stray "abbreviation for nihonium, uses 本 since 日 is taken for oxygen" note — consolidated to one bullet (the abbreviation trivia wasn't checklist-relevant and wasn't lost, just not the kind of fact this section holds). Wrote a real graphemic bullet from scratch (指事: a stroke marking the root of 木) since Notes previously had none. Words cross-check (15 total, 5 via the bare-"本" bug) found 12 missing. **Caught one more guessed-ruby near-miss**: drafted 本質 as `ㄅㄛㄋㄐㄧ`, verified against the word file and corrected to `ㄅㄛㄋㄐㄧㄊ` (missing final consonant) — 本来's guess happened to land correctly this time, but I checked it anyway rather than assuming a hit means the method is safe. No chengyu gaps (舎本逐末 already present), no `graphemic_classification: 本` hits.

**[[characters/他 (char)|他]]**: two frontmatter bugs. First, `tags:` had a stray empty list item (`- ` with nothing after it) before `- character` — cosmetic YAML noise, removed. Second, and more substantive: `graphemic_classification` was stored as `蛇` (snake), while the page's *own* Notes bullet already read "形声: semantic 亻 + phonetic 也" — same class of frontmatter/body contradiction as 市's `象形`-vs-形声 case two iterations ago. This one took an extra check because 蛇 isn't a random mismatch: `characters/蛇 (char).md`'s own MC reading (tʰ+ɑ) is an *exact* match for 他's (tʰ+ɑ), which briefly looked like it might vindicate the frontmatter over the bullet — 蛇 has a secondary "tuó" reading (as in 委蛇) that's phonetically identical to 他. But Wiktionary's actual etymology for 他 states the phonetic component is 也, not 它/蛇, despite 他 being derived from 佗 (whose own history does connect to 它/蛇 several steps removed) — corrected the field to `也` to match the verified, already-written bullet. No `graphemic_classification: 他` hits, so no Derived Characters section. Words cross-check (3 total) found 1 missing (他動詞); normalized an informally-worded callout and fixed the missing radical/SKIP/Stroke/MC/Levels bullets.

Next: 去, 右, 世, 打, 石 per `danayo_id`.

### 2026-07-16, iteration 14 — [[characters/去 (char)|去]] and [[characters/右|右]]

Next two by `danayo_id` (61, 62). Both stamped `date-last-perfect: 2026-07-16`.

**[[characters/去 (char)|去]]**: blank `pos` → `性詞`. Notes had no graphemic bullet at all (just floating CC links) — researched via Wiktionary and wrote one (会意 of 大→土 "man" + 口→厶 "opening/mouth," departing through an opening). **Caught a Levels-mapping mistake before it shipped**: first wrote `joyo_level: "3"` as `Jōyō - Kōtō` by pattern-matching against the recent 高等 case on 心 without re-checking the mapping table — "3" is a plain digit, which maps to `Jōyō - Kyōiku` like any other 1-6 value; only the literal string `高等` maps to Kōtō. Caught and fixed before finalizing. Words cross-check (4 total, 2 via the bare-"去" bug) found 3 missing. `graphemic_classification: 去` hit 却 and 怯 — both confirmed via Wiktionary as genuine members of the 去-phonetic series (OC *kʰas → *kʰaɡ/*kʰab, aspiration-preserving derivatives), included in new `## Derived Characters`. **Corpus bugs fixed**: `words/去年.md` and `words/辞去.md` had bare `- 去` instead of `- 去 (char)`.

**[[characters/右|右]]**: blank `pos` → `名詞`. Sections were reversed (Words before Notes) and Notes had zero content beyond floating CC links — wrote the graphemic bullet from scratch after confirming via Wiktionary that the stored `graphemic_classification: 又` is correct (右 was originally just 又 as a phonetic loan for "right hand," with 口 added later in bronze script purely as a distinguishing mark, not a meaning-bearing semantic radical in the usual sense — worth noting explicitly in the bullet since it's an unusual 形声 case where the "semantic" component is admittedly meaningless). Added radical line (右's own `radical: 口` field, not itself — links to Radical 030, same page 口/口's own character page points to). Words cross-check (4 total) found 2 missing plus the stand-in (右側) itself. No chengyu, no `graphemic_classification: 右` hits.

Next: 世, 打, 石, 玉, 号 per `danayo_id`.

### 2026-07-16, iteration 15 — [[characters/世|世]] and [[characters/打|打]]

Next two by `danayo_id` (63, 64). Both stamped `date-last-perfect: 2026-07-16`.

**[[characters/世|世]]**: another calendar-vocabulary-heavy page (24 words, mostly the "N-th century" set: 今世紀/前世紀/昨世紀/来世紀/etc., mirroring 月/日's month/day sets) — used the same batch-script approach for speed. Non-canonical structure throughout: `# Notes` (wrong level) held only a prose aside instead of a real graphemic bullet, `## Words using 世` (custom heading text) held 3 entries as plain markdown links missing the `../` prefix, and `### Chengyu` was H3 instead of H2. Rebuilt fully, including a graphemic bullet researched from scratch (象形: three 十 strokes = "thirty years," a generation). Words cross-check (24 total) found 16 missing including the stand-in (世界). Chengyu cross-check found 1 missing (塩地光世) alongside the 3 already present. **Caught a fabricated-content near-miss worse than the usual guessed-ruby pattern**: while placing the 2 pre-existing Chengyu entries (世仇宿敵, 世間罪盛) into ruby format, I drafted *both* empty `<rt></rt>` tags and invented English glosses out of thin air ("hereditary feud and mortal enemy", "the world overflows with sin") instead of reading the actual chengyu files — caught it before finalizing and pulled the real `注音`/`english` values (generational feud with a long-standing enemy / the world is sinful). This is a step further than previous guessing mistakes: those were wrong values pattern-matched from adjacent data, this was pure fabrication with no source at all — worth being more alarmed by. No `graphemic_classification: 世` hits.

**[[characters/打|打]]**: much smaller — graphemic bullet (形声, 扌+丁) was already correct, just needed SKIP/Stroke/Levels bullets and the MC bullet rewritten to handle `mc_id: 0` correctly per the checklist's explicit policy (a real, meaningful value meaning "confirmed absent from the ranking," phrased as "Not present in the Classical Chinese usage ranking" rather than treated as blank/an error) — first character this sweep to actually hit that case. Words cross-check (5 total) found all 5 already listed, just needed ruby/gloss on 4. Chengyu was entirely absent (一網打尽, 不打不成器) — added both. No `graphemic_classification: 打` hits.

Next: 石, 玉, 号, 外, 用 per `danayo_id`.

### 2026-07-16, iteration 16 — [[characters/石 (char)|石]] and [[characters/玉 (char)|玉]]

Next two by `danayo_id` (65, 66). Both stamped `date-last-perfect: 2026-07-16`.

**[[characters/石 (char)|石]]**: found another frontmatter/body contradiction, same class as 市/他 in earlier iterations — `graphemic_classification` said `指事`, but the page's own already-written bullet literally reads "[List of 象形]... a stone beneath a cliff," word-for-word matching Wiktionary's 象形 classification. Fixed the field to `象形`. Fixed a Words entry using a leading `/words/` path instead of `../words/` (石素). Words cross-check (20 total, 1 via the bare-"石" bug on 鑽石) found 6 missing. Chengyu section didn't exist at all despite 3 real hits (電光石火, 一石二鳥, 石山盈界) — added all 3. `graphemic_classification: 石` hit 3 characters (庶, 妬, 拓) whose Middle-Chinese-level readings in this vault's own transcription all look completely unrelated to 石's (ʑ+iᴇk vs ɕ+ɨʌ / t+uo / tʰ+ɑk) — initially suspected a repeat of the 要/表/失/絹/沛 corpus-error pattern, but checked each individually via Wiktionary and **all three came back as genuine, independently-documented phonetic derivatives** of 石 (*djaɡ), with the divergence explained by real Old-Chinese-to-this-vault's-MC sound changes rather than data errors. Kept all three — a useful counter-example to keep in mind: MC-level mismatch alone isn't sufficient grounds to exclude a candidate, unlike the previous cases where the *sources themselves* disagreed with the corpus.

**[[characters/玉 (char)|玉]]**: blank `pos` → `名詞`. Notes had zero content beyond floating CC links (wrong heading level too) — wrote the graphemic bullet from scratch (象形: depicts a 琮 ritual jade totem, or three jade pieces strung together). Words cross-check (6 total) found 3 missing (玉璽, 碧玉, 紅玉). Chengyu was entirely absent (金科玉律) — added. No `graphemic_classification: 玉` hits.

Next: 号, 外, 用, 半, 白 per `danayo_id`.

### 2026-07-16, iteration 17 — [[characters/号|号]] and [[characters/外|外]]

Next two by `danayo_id` (67, 68). Both stamped `date-last-perfect: 2026-07-16`.

**[[characters/号|号]]**: blank `pos` → `名詞`. Graphemic bullet (会意, 口+丂) already matched the stored `graphemic_classification: 會意` — checked Wiktionary anyway per the running practice and confirmed the character's real origin (a tiger roaring, exhaled-air line) is consistent with the vault's simplified 口+丂 gloss, so left it as-is. Just needed SKIP/Stroke/MC/Levels bullets. Words cross-check (9 total) found only 1 missing (注音符号) — the rest were already listed, just needed ruby/gloss. No chengyu, no `graphemic_classification: 号` hits.

**[[characters/外|外]]**: the most consequential fix this iteration. `graphemic_classification` was stored as `會意`, directly contradicting the page's own already-written bullet ("形声 (OC *ŋʷaːds): semantic 卜 + phonetic 月") — same contradiction class as 市/他/石 in prior iterations. Checked Wiktionary: 形声 with phonetic 月 is indeed the primary scholarly analysis (an alternative 会意 reading exists but isn't the standard one) — corrected the field to `月` and filled in the bullet's blank semantic gloss for 卜 ("divination," previously just empty parens). **This one has a downstream effect**: since 外's phonetic really is 月, and I'd already stamped [[characters/月 (char)|月]] as perfected two iterations ago with zero `graphemic_classification: 月` hits (because 外's field was wrong at the time) — went back and added 外 to 月's `## Derived Characters` now that the data is fixed, rather than leaving a stale gap in an already-"complete" page. Worth remembering for the rest of this sweep: fixing a wrong `graphemic_classification` can retroactively create a Derived-Characters gap on a *different*, already-perfected page — check for that ripple, don't just fix the page in front of you. Words cross-check (14 total) found 8 missing including the stand-in (外部). Chengyu (内柔外剛) was present but glossless — added the gloss.

Next: 用, 半, 白, 主, 立 per `danayo_id`.

### 2026-07-16, iteration 18 — [[characters/用|用]] and [[characters/半|半]]

Next two by `danayo_id` (69, 70). Both stamped `date-last-perfect: 2026-07-16`. Both had the same defect: `graphemic_classification` stored as `象形`, while the actual bullet — broken/templated-but-empty on 用, entirely absent on 半 — pointed toward something else once researched. Third and fourth instances of the frontmatter-vs-reality contradiction pattern (after 市, 他, 石, 外), and both required real research since neither page's existing text gave the correct answer outright this time.

**[[characters/用|用]]**: blank `pos` → `事詞`. The Notes bullet was a literal unfilled template — "形声 (OC ): semantic [[]] ("") + phonetic [[]] (OC )." — with every field empty. Wiktionary confirms 用 is 形声, "a handle added to 同, which also supplies the phonetic value" (not a semantic+phonetic pair with a separate semantic radical — 同 alone does both jobs, similar structurally to 友's 亦聲 case). Fixed `graphemic_classification` from `象形` to `同` and wrote the bullet for real. Words cross-check (22 total) found 11 missing including the stand-in (使用). No `graphemic_classification: 用` hits. **Incidental fix**: `words/副用.md` had `english: auxillary` (typo → auxiliary).

**[[characters/半|半]]**: Notes had zero content beyond floating CC links. Wiktionary confirms 半 is 会意 (八 "divide" + 牛 "cow" — cutting a cow in half), not 象形 — fixed the field and wrote the bullet. Words cross-check (6 total) found 5 missing including the stand-in (一半). `graphemic_classification: 半` hit 絆 — exact MC match (p+uɑn both), included in new `## Derived Characters`. No chengyu.

Next: 白, 主, 立, 母, 正 per `danayo_id`.

### 2026-07-16, iteration 19 — [[characters/白 (char)|白]] and [[characters/主|主]]

Next two by `danayo_id` (71, 72). Both stamped `date-last-perfect: 2026-07-16`.

**[[characters/白 (char)|白]]**: blank `pos` → `名詞`. Notes had a stray "abbreviation for platinum" note instead of a real graphemic bullet — same relocate-into-Words pattern as 田/本's duplicate-entry fixes. Words cross-check (21 total, 2 via the bare-"白" bug) found 3 missing (白止, 白頭翁, 白頭鷲) and ruby on ~9 bare entries. Chengyu was entirely absent (白頭偕老) — added. `graphemic_classification: 白` hit 4 characters (柏, 百, 珀, 魄) — all confirmed as a real, well-documented phonetic family (labial-initial + -æk/-ak finals), included in new `## Derived Characters`. **Caught a genuine fabrication, not just a near-miss — this one actually got written to the file before I caught it**: for the graphemic bullet I wrote "uncertain origin; interpretations include a thumbnail, a rice grain, or the whitened top of a candle flame" entirely from imagination, without checking Wiktionary at all, since the frontmatter's `象形` needed no contradiction-resolution this time and I got complacent. Checked Wiktionary afterward as a routine pass and found the real proposals are completely different (an acorn, comparing to 櫟; or a human head, comparing to 貌/黑) — corrected in place. This is a step past every previous near-miss in this log: those were guessed values later verified before finalizing, this was fabricated content that was actually saved to disk and would have shipped if I hadn't independently decided to double-check a bullet that had no obvious red flag. **New rule for the rest of this sweep: every graphemic bullet gets a Wiktionary check before being written, full stop — not just the ones where the frontmatter already looks contradictory.**

**[[characters/主|主]]**: the existing graphemic bullet was garbled mid-sentence ("Three distinct forms can be observed in ancient scripts... :.", trailing off with stray colons and no actual list of the three forms) — checked Wiktionary and found it was quoting the real source correctly right up to where it got cut off; completed it properly (lamp/torch with flame; wood on fire, now 王; a spirit tablet). Fixed two link bugs: `[[../words/棕枝主日]]` (prefix-inside-wikilink, same bug class fixed on 日 two iterations ago) and `[民主](/words/民主.md)` (leading-slash path). Words cross-check (29 total) found 11 missing including the stand-in (主人). Chengyu was entirely absent (主宰万物, 愛主耳錐) — added both. `graphemic_classification: 主` hit only 注 — exact MC match (t͡ɕ+ɨo both), included in `## Derived Characters`.

Next: 立, 母, 正, 左, 生 per `danayo_id`.

### 2026-07-16, iteration 20 — [[characters/立 (char)|立]] and [[characters/母|母]]

Next two by `danayo_id` (73, 74). Both stamped `date-last-perfect: 2026-07-16`. First iteration applying the new post-fabrication rule: every graphemic-bullet claim checked against Wiktionary before writing, not just the ones that already looked contradictory.

**[[characters/立 (char)|立]]**: closest to already-complete of any page this sweep — existing graphemic bullet (a standing person, compare 大/文, contracted-form note re 辛/妾/童, unrelated to 音) checked against Wiktionary and confirmed accurate, kept verbatim. Real defects were structural (MC/SKIP/Levels bullets out of order, Chengyu before Words) plus one factual slip: the Levels bullet said `Old HSK 2` but the frontmatter's own `hsk_level` is `"1"`, which maps to `HSK Beginner` — fixed. Words cross-check (11 total, 2 via the bare-"立" bug) found 7 missing. Chengyu cross-check found 2 more (孤立無援, and — via a bare-"立" bug on the chengyu side too — 家分不立) alongside the 3 already listed, which were present but glossless. `graphemic_classification: 立` hit 5 characters (拉, 翌, 颯, 粒, 雴); checked each individually and **excluded 翌** — Wiktionary gives its real phonetic component as 羽, not 立, a total mismatch (OC *lɯɡ vs 立's *rɯb) — 立 has no relationship to 翌 at all (not even semantic), a genuine corpus-error case like 絹/月, distinct from the semantic-only 表/失/集 cases that were wrongly excluded and later corrected (2026-07-17). Kept the other 4, including 颯 despite an initially-confusing failed Wiktionary fetch (wrong character page) — retried with the correct URL and got a clean confirmation. **Corpus bugs fixed**: `words/創立.md`, `words/孤立.md`, and `chengyu/家分不立.md` all had bare `- 立` instead of `- 立 (char)`.

**[[characters/母|母]]**: Notes had nothing but a bare `[[Stroke 05]]` link — wrote the graphemic bullet from scratch after Wiktionary research (母 is a differentiated form of 女 with two dots added for breasts; oracle bone script didn't distinguish the two). Words cross-check (23 total) found only 3 truly missing (字母, 母指, 父母) since most of the page's ~17 other entries were already present, just lacking ruby. **Caught real fabrication while adding those ruby-only entries, not just guessed rt text this time but invented gloss detail**: wrote parenthetical kinship specifics ("aunt (father's younger brother's wife)" for 叔母, "(father's elder brother's wife)" for 伯母, "(mother's sister)" for 姨母, "(father's sister)" for 姑母) pulled from general knowledge of Chinese kinship terminology, not from the word files' actual stored `english:` fields, which just say plain "aunt" / "aunt, maternal aunt" / "paternal aunt" — corrected all four to match the source exactly. Also caught a straight typo of my own making: typed "father" instead of "mother" for 母親's gloss while formatting the stand-in note. Chengyu (尊敬父母) was already present, just needed formatting. `graphemic_classification: 母` hit only 毎 — not a classic phonetic pair (Wiktionary describes it as a graphic simplification of 每, 母→毋, rather than a 形声 compound) but the OC roots are near-identical (*məʔ vs *mɯːʔ) and the vault's own field already encodes it as such, so kept it in Derived Characters with that caveat noted here rather than in the page itself.

Next: 正, 左, 生, 竹, 毎 per `danayo_id`.

### 2026-07-16, iteration 21 — [[characters/正 (char)|正]] and [[characters/左|左]]

Next two by `danayo_id` (75, 76). Both stamped `date-last-perfect: 2026-07-16`.

**[[characters/正 (char)|正]]**: existing graphemic bullet (形声, phonetic 丁 + semantic 止, original form of 征) checked against Wiktionary and confirmed accurate — kept verbatim, per the new always-verify rule. First character this sweep where `words/正.md` genuinely exists alongside the character page (正 is its own stand-in, `stand_in: "正"`), confirming the existing callout was already correctly present, not missing. Words cross-check (18 total, 8 via the bare-"正" bug) found 11 missing. Chengyu cross-check found 光明正大 and 朝鮮正音 missing alongside 公明正大 (present but as a floating unstructured line, not a proper bullet). `graphemic_classification: 正` hit 3 characters (定, 征, 症) — all confirmed via Wiktionary as genuine phonetic derivatives despite MC-level divergence in two of the three, consistent with the now-familiar "trust OC over this vault's MC transcription, but verify every single time" pattern. **Corpus bugs fixed**: 8 word files (校正, 正割, 正午, 正大, 正弦波, 正確, 正餐, 真正) and `chengyu/朝鮮正音.md` all had bare `- 正` instead of `- 正 (char)` — note 正午.md's *other* side (午) was already fixed during the 午 iteration; this fixed the 正 side that iteration didn't touch, a reminder that a bare-filename bug can exist on either side of a compound independently.

**[[characters/左|左]]**: no existing bullet to check — wrote one after Wiktionary research. 左's real etymology is more nuanced than a clean 会意/形声 split: Wiktionary calls it *both* simultaneously (phonetic 𠂇 "left hand" + semantic 工 "work"), but 𠂇 has no character page anywhere in this vault to link, so kept `graphemic_classification: 會意` as the closest storable bucket and wrote the phono-semantic nuance into the bullet's prose instead of trying to force a link that doesn't resolve to anything. Words cross-check (4 total) found 2 missing including the stand-in (左側). No chengyu, no `graphemic_classification: 左` hits.

Next: 生, 竹, 毎, 気, 年 per `danayo_id`.

### 2026-07-16, iteration 22 — [[characters/生|生]] and [[characters/竹 (char)|竹]]

Next two by `danayo_id` (78, 79). Both stamped `date-last-perfect: 2026-07-16`.

**[[characters/生|生]]**: another frontmatter/body contradiction (`graphemic_classification: 象形` vs. the already-written bullet's "会意 of 屮+一") — Wiktionary confirms 会意 is correct, fixed the field. Words cross-check (30 total, confirmed no bare-filename bug this time since 生's own file has no `(char)` suffix and no collision word exists) found 13 missing and ruby on 13 existing bare entries. Chengyu cross-check found 3 more (剣生剣死, 生机勃勃, 起死回生) alongside 万物生長. `graphemic_classification: 生` hit 4 characters (星, 甥, 省, 青) — checked the least-obvious one (青, affricate initial vs 生's fricative) via Wiktionary and confirmed it's explicitly documented as phonetic-generic (生 co-phonetic with 井), kept all four in Derived Characters.

**[[characters/竹 (char)|竹]]**: smaller page — Notes had zero content beyond floating CC links; wrote the graphemic bullet after Wiktionary confirmation (two bamboo stalks with leaves, matches the stored `象形`). Added missing radical line (Radical 118). Words cross-check (4 total, 1 via the bare-"竹" bug) found 2 missing. No chengyu, no `graphemic_classification: 竹` hits.

Next: 毎, 気, 年, 肉, 多 per `danayo_id`.

### 2026-07-16, iteration 23 — [[characters/毎 (char)|毎]] and [[characters/気 (char)|気]]

Next two by `danayo_id` (80, 81). Both stamped `date-last-perfect: 2026-07-16`.

**[[characters/毎 (char)|毎]]**: `graphemic_classification: 母` was already correct (confirmed last iteration when checking 母's own Derived Characters) — this iteration wrote the actual bullet content for the first time (形声/会意 hybrid, semantic 屮 + phonetic 母, OC roots nearly identical) and built out the rest of the page from a near-empty Notes section. Words cross-check (14 total, 1 via the bare-"毎" bug) found 13 missing — nearly the entire Words section had to be built from scratch. Chengyu (毎字明意) was entirely absent. `graphemic_classification: 毎` hit 3 characters (侮, 梅, 海) — all confirmed via Wiktionary as genuine phonetic derivatives of 每/毎 (OC *mɯːʔ, exact match on all three), included in new `## Derived Characters`. **Corpus bugs fixed**: `chengyu/毎字明意.md` and `words/毎世紀.md` had bare `- 毎` instead of `- 毎 (char)`.

**[[characters/気 (char)|気]]**: found a self-referential `graphemic_classification` bug — the field literally said `気` (i.e., claiming 気 is its own phonetic component), which can't be right by construction. Checked Wiktionary: 気/氣 is 形声 with semantic 米 ("rice") + phonetic 气 (the bare ancestor pictograph of rising vapor, already listed among 気's own `aliases`) — corrected the field to `气`. Since 气 has no character page of its own in this vault (it's 気's historical root, not a separate unrelated glyph — different situation from the 巿/𠂇 no-page cases), described it in the bullet's prose without a wiki-link, matching the precedent set on 左 for an unlinkable phonetic component. Words cross-check (16 total, 2 via the bare-"気" bug) found 9 missing. Chengyu (意気揚揚) was entirely absent, and its own `characters:` field had the same bare-"気" bug. `graphemic_classification: 気` hit only 汽 — exact MC match (kʰ+ɨi both), included in Derived Characters. **Corpus bugs fixed**: `chengyu/意気揚揚.md`, `words/大気圏.md`, and `words/客気.md` all had bare `- 気` instead of `- 気 (char)`.

Next: 年, 肉, 多, 糸, 回 per `danayo_id`.

